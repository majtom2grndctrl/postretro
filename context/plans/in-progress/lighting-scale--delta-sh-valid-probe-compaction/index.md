# Lighting Scale — Delta-SH Valid-Probe Compaction

> **Status:** ready. Reviewed through validate-plan (direction), review-draft-spec (two rounds: broad · source-anchor · temporal, then a source-anchor + broad verification pass), and review-implementability; awaiting `/orchestrate`. Anchored to shipping-accurate measurements from the corrected `--sh-analyze` spike (emitted-set delta bytes; id 41 47.9→26.4 MB, total deltas 126.4→69.0 MB, 0.55×). `context/lib/` updates land at implementation, not here.
> **Track:** Lighting / build pipeline — extend the shipped base-atlas valid-probe compaction to the three sparse SH delta sections.
> **Related:** `context/lib/build_pipeline.md` §PRL section IDs (ids 27/41/45, 34/35) · `context/lib/rendering_pipeline.md` §4 ("Animated SH delta volumes", "Animated direct SH", "Promoted static lights"), §7.1 step 5 · `context/lib/experimental_spikes.md` · shipped precedent `lighting-scale--sh-base-atlas-at-rest-slimming` (main) · stopped successor `context/research/archived-plans/lighting-scale--adaptive-base-probe-density/` (density coarsening) · sibling `research.md`.

## Goal

The shipped `sh-base-atlas-at-rest-slimming` compacts the BASE indirect octahedral atlas (id 34) to valid-probe-only tiles. The three sparse SH **delta** sections do not: id 27 `DeltaShVolumes`, id 41 `DirectShDeltaVolumes`, and id 45 `AnimatedDirectShDeltaVolumes` each store `affinity_lights.len() × PROBES_PER_CELL(64) × stride` — every retained `(affinity-cell, light)` CSR entry carries all 64 probe slots, including in-solid (invalid) probes that carry no signal and are never read on the indirect path. This spec compacts each retained entry to its cell's **valid probes only**, losslessly, in a named post-drop compaction pass (the bake keeps emitting dense-64; see Direction). The composed atlases and every SH sampler are untouched *as code*: the change is confined to the delta storage and the three compose passes that read it. Parity scope differs by path — the id 27 **indirect** composed atlas stays **full-atlas** f16-bit-identical (it stores 0 at invalid probes both ways, and its compose early-outs invalids via the base `probe_indirection` word); the id 41 **direct** and id 45 **animated-direct** composed atlases are f16-bit-identical **only at valid-probe texels**, because those passes have no invalid early-out and legitimately write `base` at invalid direct texels after compaction. That is safe because the SH sampler zero-weights invalid corners (`sh_sample.wgsl` validity-weighting, ~`:157,249`) — the sampler is not edited, but the parity gate depends on it (see AC gate 1, D2, C1).

## Scope

### In scope

- **Compact each retained delta CSR entry to valid-probes-only,** in a new named post-drop compaction pass (`apply_valid_probe_compaction`, `crates/level-compiler/src/delta_sections.rs`; see Direction), not in the bake. Per-entry payload becomes `valid_count(cell) × tile_stride` instead of `64 × tile_stride`. Within-cell storage order is the existing x-fastest local order, filtered to valid probes.
- **Three sections:** id 27 (indirect), id 41 (direct), id 45 (animated-direct). Section-internal version bump each; loader validation; fail-loud rejects; no compat shim.
- **A per-cell valid-probe descriptor** carried by each delta section (self-validating; carrier resolved — see Direction and Task 1) so the compose pass resolves `(cell, local_probe) → stored slot | absent`, and so the loader cross-checks it against the id-34 validity that is the single source of truth and fails loud on any disagreement.
- **Compose-pass resolution** in the three delta compose shaders (`sh_compose.wgsl`, `direct_sh_compose.wgsl`, `animated_direct_sh_compose.wgsl`), their render-cpu buffer builders, and the `renderer` crate wgpu wiring (the descriptor + entry-offset prefix are packed into ONE combined storage buffer per pass, created and bound in `crates/renderer/src/render/sh_compose.rs`, Renderer-owns-GPU). The composed (total/direct) atlases keep their dense geometry; samplers keep arithmetic reads.
- **Bake + compaction sites.** The bake sites `delta_sh_bake.rs` (id 27), `direct_sh_bake.rs` (id 41), `animated_direct_sh_bake.rs` (id 45) keep emitting dense-64; the compaction pass consumes the post-drop section and emits the compacted section + descriptor. The 64-probe wire identities (`crates/level-format/src/delta_sh_volumes.rs` `to_bytes`/`from_bytes` and siblings; `direct_sh_delta_has_valid_csr_shape` in `crates/level-compiler/src/pack.rs`) become the summed per-cell identity over the compacted on-disk section.
- **Interaction with the exact-zero drop policy and the payload cap** (`crates/level-compiler/src/delta_sections.rs`, `delta_drop_policy.rs`): the exact-zero drop runs first and **unchanged** on dense-64; the compaction pass runs after it; only the payload cap (`enforce_payload_cap`) accounts compacted bytes. The SET of dropped entries is identical whether the drop sees dense-64 or compacted input (invalid probes are dead-zero), so the ordering is mechanics, not behavior drift. Model this in the value framing and ACs.
- **Measured before→after** via the shipped `--sh-analyze` instrument (`(a)compacted` byte line) and `POSTRETRO_GPU_TIMING`.

### Out of scope (non-goals)

- **Density coarsening / adaptive per-brick level (64/8/1).** Dropping *valid* probes is lossy and owned by the stopped-and-reopened successor (`archived-plans/lighting-scale--adaptive-base-probe-density`). This spec drops only invalid probes. See Direction for the seam.
- **Composed-atlas compaction and the sampler resolve rework.** The total/direct composed atlases stay dense; `probe_tile_origin` and `sh_sample.wgsl`'s corner walk are untouched. That whole-SH-reader rework is coarsening's, not compaction's (Direction).
- **id 35 `DirectShVolume` (dense-BC6H base for the direct compose).** Its at-rest compaction is not part of this spec; the direct compose reads it dense today and continues to. Only the delta payload it companions is compacted.
- **id 42 `ShadowmaskAtlas` (`shadowmask_bake.rs`).** Despite being listed as a bake site in the task framing, id 42 is a per-texel world-visibility atlas (`width × height × layer × RGBA`, `crates/level-format/src/shadowmask_atlas.rs`), not affinity-cell × 64-probe. It has no invalid-probe axis to compact. Excluded, with cause.
- **A cap-selection / drop-to-fit-by-light policy.** The current cap is a hard error, not a selection (Direction, Open Questions). Building one is a separate owner decision.
- **New per-frame runtime knobs.** No user-facing settings; measurement is log-line / dev-tools footprint only.

## Direction

**Problem (cause, not symptom).** The delta sections store a fixed 64 probes per retained CSR entry regardless of how many are valid. On `stress-warren-showcase.map` at 2 m spacing only 59% of base probes are valid (67,838 / 115,200); near-surface affinity cells — where deltas concentrate — carry a large in-solid fraction. Every invalid slot is dead weight in `.prl`, in the GPU delta storage buffer at rest, and in the bytes the compose pass streams for each active light. The base atlas already pays this only for valid probes; the deltas do not. Cause: the delta payload stride is `PROBES_PER_CELL`, a compile-time constant, not the cell's valid count.

**How this is priced — honestly (read this before claiming a win).** Measured on the stress map (`--sh-analyze`, the just-completed spike, using the emitted/shipped delta set — post static-light selection and exact-zero drop; an earlier tool version measured a ~4.6× inflated pre-filter superset, since corrected): id 41 shipped delta payload 47.9 MB → valid-probe-compacted 26.4 MB (0.55×); id 27 22.3 → 11.8 MB (0.53×), id 45 20.9 → 11.3 MB (0.54×); total deltas (ids 27/41/45) 126.4 MB → 69.0 MB (0.55×). Holding map content fixed, compaction reduces three things by roughly that ratio: (1) `.prl` on-disk delta bytes; (2) at-rest delta VRAM (the `delta_subblocks` storage buffers, `ComposeStorageFootprint.delta_subblocks_bytes`, `crates/render-cpu/src/sh_compose.rs`); (3) per-frame compose traffic (the compose reads each active entry's payload; smaller entries = fewer bytes read).

The honest caveat on (3): whether the per-frame reduction *materializes as bandwidth saved* or *as quality gained* depends on author behavior against the delta payload cap (`DEFAULT_MAX_PAYLOAD_BYTES = 256 MiB`, `delta_sections.rs:15`). If content is held fixed, all three wins are real and the cap does not bind (measured shipped id 41 ≈ 48 MB after exact-zero drop, far under 256 MiB). If authors instead bake maps up to the cap ceiling — pushing light count and probe density until the aggregate delta payload nears the cap — the headroom compaction frees is spent on more lights / finer density, and the realized win is **higher lighting quality at roughly equal shipped bytes and equal per-frame traffic**, not reduced traffic. Both are true; they are different content counterfactuals. This spec therefore **measures** the byte/VRAM/traffic deltas and **records** them (spike doctrine: findings, not gated targets); it does not contract a per-frame-bandwidth target as a deliverable, and it does not claim the quality win as automatic — that win is an author choosing to spend the headroom.

**Correction to the "cap that drops whole lights" framing.** The shipped cap (`enforce_payload_cap`, `delta_sections.rs:125`) is a **hard, named compile error** when the aggregate exceeds the cap — it does not fit maps by dropping lights, and it performs no light selection. The only shipped dropping is `apply_exact_zero_drop_policy` (`delta_sections.rs:69`), which removes CSR entries whose entire 64-probe payload decodes to exactly zero — lossless, and unrelated to the cap (it runs regardless). So "more lights survive the cap's selection" does not describe current behavior: there is no selection. What compaction actually does to the cap is **raise the light/density ceiling that compiles under a fixed cap** (each retained entry is ~0.55× smaller, so ~1.8× the entries fit before the hard error). Whether the project wants a future *drop-to-fit-by-selection* policy is an owner decision recorded in Open Questions; it is not built here and this spec does not depend on it.

**Prior commitments this touches.**
- **Base-atlas at-rest slimming (shipped, main).** Established valid-probe compaction for id 34, the v9 `SH_VOLUME_VERSION` with a reserved-zero per-probe `density_level`, and `build_probe_indirection_words` (`crates/renderer/src/render/sh_compose.rs:465`) — one `u32` per nominal probe carrying the probe's **global** compact slot or `INVALID_PROBE_INDIRECTION`. This spec reuses that feature's validity source and load-time-derivation pattern (see below) and its fail-loud/no-compat-shim posture (`development_guide.md §1.6`).
- **The affinity CSR substrate.** `AFFINITY_FACTOR = 4`, `affinity_dims = ceil(base_dims / 4)`, CSR `affinity_offsets`/`affinity_lights`, x-fastest in-cell probe order. Unchanged; compaction changes only per-entry payload length and its indexing.
- **Exact-zero drop + hard-error cap.** The exact-zero drop stays first and **literally unchanged**, operating on dense-64 (it removes whole-cell zero entries); the named compaction pass (`apply_valid_probe_compaction`) runs after it and shrinks the survivors; only the hard-error cap is re-accounted to compacted bytes. The dropped-entry SET is identical whether the drop sees dense-64 or compacted input (invalid probes are dead-zero), so this ordering is mechanics, not behavior. Full pipeline: bake(dense-64) → exact-zero drop → compaction pass → `enforce_payload_cap`. `enforce_payload_cap` is currently invoked as the tail call of `apply_exact_zero_drop_policy`, not a sequential sibling; inserting the compaction pass between them requires lifting the `enforce_payload_cap()` call out of `apply_exact_zero_drop_policy` (or invoking compaction inside it before the cap) — trivial, `enforce_payload_cap` is a separate `pub(crate)` fn. The per-cell descriptor is threaded through the drop→compaction reconstruction unchanged (validity is per-cell; dropping an entry never changes a cell's mask), and its popcount is what the pack-time summed-identity check (`direct_sh_delta_has_valid_csr_shape` and siblings) reads.
- **f16 at rest / f32 arithmetic.** Payloads stay `Rgba16Float`, read through `unpack2x16float`; no WGSL `enable f16`. Preserved.

**Reuse vs. parallel indirection — and why the shared base word cannot directly serve.** The task asks: reuse the base-atlas indirection-word infra, don't invent a parallel one unless justified. The justification exists and is structural. `build_probe_indirection_words` yields each probe's **global** valid-rank (its slot among all valid probes, whole-grid x-fastest). The delta needs each probe's **within-cell** valid-rank (its slot among the valid probes of its own 4×4×4 cell) plus the entry's payload **base offset** (pinned as a CPU-precomputed per-entry offset array carried in a buffer — see the per-entry-offset Alternative below). A cell's 64 probes are **non-contiguous** in whole-grid x-fastest order, so within-cell rank is not a cheap function of global rank — the shared word is the wrong rank space. What *is* reused: the **validity source** (id-34 probe metadata validity, "the sole source of truth"), the **load-time derivation pattern** (`build_probe_indirection_words`-style, deriving from id-34, never serialized as new PRL bytes on the runtime side), and the **compose-only carrier** shape. The delta's own resolver is a per-cell valid-probe descriptor (recommend a 64-bit mask per affinity cell): `is_valid(local) = (mask >> local) & 1`; `within_cell_rank(local) = countOneBits(mask & ((1u64 << local) - 1))`; `valid_count(cell) = countOneBits(mask)`. The mask doubles as the section's self-validation carrier (its popcount fixes the entry payload length) and as the compose-time resolver — one artifact, no separate side table. This is a sibling of the base word built by the same infrastructure, justified because the base word's rank space does not answer the delta's question.

Additional structural note the base word does not cover: the id 41 / id 45 direct compose passes (`direct_sh_compose.wgsl`, `animated_direct_sh_compose.wgsl`) read a **dense** direct base atlas (id 35) and today bind **no** per-probe indirection at all — unlike the indirect compose, they have no invalid-probe early-out to reuse. Compaction introduces the per-cell descriptor into those passes for the first time.

**Alternatives rejected.**
- *Merge into density coarsening (make it one spec).* Rejected. Coarsening is lossy (drops valid probes; needs an error/seam budget, a gate spike, and manual A/B) and reworks every SH sampler plus the composed atlases; compaction is lossless (drops only zero-signal probes; f16-exact parity is the gate) and touches only the compose internals. Merging blocks a shippable lossless win behind a stopped/reopened lossy plan (the successor is archived pending a research spike). Compaction is the clean prerequisite that pre-builds coarsening's variable-stride delta substrate. Full argument and the exact seam handed forward: `research.md` §Separability.
- *Per-entry offset table instead of a per-cell descriptor.* Rejected as a **replacement** for the descriptor: the mask carries validity (`is_valid`/`within_cell_rank`) and self-validates the payload length, which a bare offset table cannot, and it does not answer `is_valid(local)`. The two are complementary, not alternatives. The entry payload **base offset** is **not** shader-summed: it is pinned as a **CPU-precomputed per-entry offset array**, derived at LOAD from the final post-drop CSR in entry order (== payload write order, x-fastest within cell) and carried in a buffer the shader indexes as `offset[entry]`, for all three sections. The compose resolver is `offset[entry] + within_cell_rank(local) × stride`: the array supplies the base, the mask supplies the in-entry rank and validity. The offset array never replaces the validity carrier.
- *Ship id 41 only, defer id 27/45.* id 41 dominates (47.9 MB vs 22.3/20.9 MB for id 27/45 on the stress map; the gap widens on lighter maps) and Task 2 proves the whole substrate on it alone. Rejected: id 27/45 are pure fan-out over the proven slice (Tasks 3–4, cheap), and leaving them at 64-probe stride strands a permanent format asymmetry that the density-coarsening successor — which needs all three sections variable-stride — would have to finish anyway. Do all three now.
- *Compact the composed atlases too, "while we're here."* Rejected — that forces every sampler through a resolve, is coarsening's blast radius, and buys nothing for the delta (the composed atlas is written by compose and read by samplers arithmetically; the delta storage is the only place invalid probes cost bytes). Kept as an explicit non-goal.
- *Derive validity at load only from id 34, carry nothing in the delta section.* Rejected for fail-loud: a delta section that cannot validate its own payload length standalone, and cannot be cross-checked against id-34 validity, would misalign the CSR tail silently on any disagreement. Each section self-describes its per-cell descriptor and the loader cross-checks it against id-34 (mirrors the successor's base-vs-delta cross-check posture).

**One-way doors / foreclosures.** The section version bumps are one-way (no compat shim, by house posture) — a pre-bump `.prl` rejects with the named recompile error. That is cheap to walk back (a re-bake), matching the existing delta-format and at-rest-slimming posture. Nothing here forecloses density coarsening; it is designed to be coarsening's substrate. Nothing forecloses a future cap-selection policy. No runtime-observable behavior changes for shipped maps beyond identical lighting at fewer bytes (the parity gate).

## Acceptance criteria

Per `experimental_spikes.md`, split into honesty gates (automated: correctness/rejects/parity) and measured findings (recorded, not thresholds to iterate toward). This is a hardened lossless feature; parity is the load-bearing guarantee — but which gates actually run in *this* project's headless CI, versus dev/local or review, is made explicit below so no gate reads as more enforced than it is.

**CI-enforceability ledger** (a gate that "self-skips headless" is a dev/local check, not a CI guarantee — do not mistake one for the other):

| Gate | Enforcement |
|---|---|
| Lossless parity — oracle (a): CPU compaction + resolver **value** check | CI-hard |
| Lossless parity — oracle (b): GPU composed-atlas readback | dev/local (self-skips headless) |
| Invalid-direct-texel safety — source-guard | CI-hard |
| Invalid-direct-texel safety — GPU-readback behavioral | dev/local (self-skips headless) |
| Self-consistent sections | CI-hard |
| Base↔delta cross-check reject | CI-hard (compiler/loader unit) |
| Cap + exact-zero re-accounted | CI-hard |
| No regression — combined-buffer layout count test | CI-hard |
| No regression — boot, no wgpu validation errors | dev/local (boot; self-skips headless) |
| No regression — samplers / consumer-stage counts unchanged | review/grep gate |
| Measured findings (M1–M5) | recorded, not gated |

**Honesty gates:**

- [ ] **Lossless parity.** For each of the three sections, on `content/dev/maps/stress-warren-showcase.map` and `content/dev/maps/campaign-test.map`, the composed atlas is **f16-bit-identical** to a dense-64 reference for the same bake inputs, scoped per path: **full-atlas** for id 27 (indirect total — it stores 0 at invalid probes both ways, and the compose early-outs invalids via the base `probe_indirection` word, binding 26); **only at valid-probe texels** for id 41 (direct composed) and id 45 (animated-direct), whose passes have no invalid early-out, so after compaction an invalid direct texel legitimately becomes `base`. Writing `base` there is safe because the SH sampler zero-weights invalid corners (`sh_sample.wgsl` validity-weighting, ~`:157,249`) — named explicitly, though samplers are otherwise out of scope, because the implementer must trust it to freely write `base` at invalid direct probes and to trust the gate. Parity holds because compaction drops only probes never read at a valid texel and reorders nothing a reader sees. **Two pinned oracles, not one:** (a) a pure-CPU unit test that runs `apply_valid_probe_compaction` over a mixed-validity fixture and asserts, **byte-for-byte**, that every valid probe's tile survives at its resolved slot (`offset[entry] + within_cell_rank(local) × stride`) and every invalid probe is absent — plus the descriptor-popcount payload-length identity. CI-unconditional; catches any compiler-side compaction *value* bug, not just addressing. It exercises the compaction + resolver model, not the shipped WGSL read (that is oracle b's job); (b) a **GPU-dispatch-and-readback** parity test composing the real WGSL both ways (dense-64 reference vs compacted) and asserting f16-bit-identity — full-atlas for id 27, valid-probe-texels (id-34/descriptor-mask-selected) for id 41/45 — which SELF-SKIPS on a headless/no-adapter host (precedent: `curve_eval_test.rs`; `testing_guide.md §3` notes headless CI is the norm). No compat shim exists, so "compose both ways" is reference-vs-compacted, not old-loader-vs-new. **Weights:** id 41 is promotion-gated (`selection_weights`), so its parity read requires a nonzero **promotion** weight (at-rest all-zero would make the id-41 read never run — a vacuous pass); id 27/45 use a nonzero **animation** weight. **CI-enforceability, stated plainly:** CI unconditionally enforces the resolver model (a) plus section self-consistency; the full composed-atlas parity (b) is a dev/local gate that self-skips in headless CI. This is the honesty-gate's real boundary — this AC does not claim "CI-enforced parity" beyond that.
- [ ] **Invalid-direct-texel safety, gated (closes the valid-texel-scope hole).** The id 41/45 valid-texel parity scope deliberately excludes the invalid direct texels that now hold `base`; their safety rests entirely on the SH sampler zero-weighting invalid corners (`sh_sample.wgsl` — `validity = select(0.0, 1.0, is_valid)` scaling the direct radiance in `sample_sh_indirect_direct_corners`). That dependency is currently untested (`sh_sample.wgsl` carries only compile-coverage in `shader_tests.rs`), so a future sampler edit would break this spec's losslessness with no failing test. No committed-golden infra exists (`capture-animated-direct-receiver-goldens` is an unbuilt draft), so gate it at the strongest level available now with two guards: **(a)** a CI-unconditional source-guard test asserting `sh_sample.wgsl` retains the invalid-corner zero-weight term applied to the direct radiance — catches accidental removal even headless; **(b)** a GPU-readback behavioral test (`gpu_test_harness`, self-skips headless like the parity oracle (b)) that a fragment whose nearest direct corner is invalid yields the identical sampled result whether that texel holds `base` or a poisoned value — proving invalid direct texels never reach a sampled fragment. Guard (a) is the CI-unconditional regression tripwire for the C1 valid-texel scope; (b) is the dev/local behavioral proof.
- [ ] **Self-consistent sections.** Both maps compile end-to-end to `.prl`s whose delta sections are internally consistent: every retained entry's payload length equals `valid_count(cell) × tile_stride`, the per-cell descriptor's popcount matches, entry order and CSR shape are unchanged, and the empty-cell / dropped-entry absence encoding is unchanged. Asserted at bake, self-validated at parse.
- [ ] **Base↔delta validity agreement, fail-loud.** The loader cross-checks each delta section's per-cell descriptor against id-34 probe validity for the same cell **before** the render-cpu builder sizes or fills the compacted buffer or derives the entry-offset array (that buffer build happens between parse and dispatch), never a wgpu validation crash. A descriptor↔id-34 disagreement is corruption or version-skew: for **id 27 and id 45** it is a **named hard load error, all-or-nothing** (nothing composed). For **id 41** it clears the existing all-or-nothing selection set — id 40 `EntityShadowLights` + id 41 `DirectShDeltaVolumes` + id 42 `ShadowmaskAtlas` together — matching the existing direct-path posture, and the compacted buffers are never built. A pre-bump section version rejects with the named recompile error; no compat shim.
- [ ] **Cap + exact-zero drop re-accounted.** The exact-zero drop still removes whole-zero entries first, **unchanged on dense-64**; the compaction pass runs after it; the payload cap (`enforce_payload_cap`) counts **compacted subblock bytes only** (the per-cell descriptor is excluded from the cap); over-cap is still the named hard error naming all three ids. A fixture set below cap compiles; a fixture with the cap set below the compacted need fails loud. No silent degrade.
- [ ] **No budget / binding regression.** Both maps boot to first rendered frame with no wgpu validation errors (**boot gate**, not a unit test); the composed atlas geometry and the sampler bind groups are unchanged (**grep/review gate**: negative-existence claim, no test asserts an absence of change). Per-stage storage/sampled-texture counts are unchanged **at the consumer/sampler stages** (forward, fog, billboard, skinned, kinematic) — compaction never reaches them (**grep/review gate**). Each of the **three compose passes** legitimately gains exactly **ONE** new storage buffer (the per-cell descriptor and per-entry offset array packed into a single combined `BIND_DELTA_COMPACTION_META` buffer per pass); that is expected, not a regression (**unit test**: the layout test below). The indirect pass lands at 7→8 compute storage buffers — the 8-per-stage downlevel ceiling (`maxStorageBuffersPerShaderStage = 8`), zero remaining headroom — and the `compose_layout_keeps_seven_compute_storage_buffers_and_local_sampler` test expectation updates to eight for the indirect pass (**unit test**). Final counts stated in the findings note.

**Measured findings (recorded as before→after with ratio against the uncompacted baseline; not gated):**

- [ ] Per-section delta bytes (ids 27/41/45) and total `.prl`, both maps, via `--sh-analyze`'s `(a)compacted` line cross-checked against the emitted section sizes. Anchor (measured, corrected emitted-set tool): id 41 stress 47.9 → 26.4 MB (0.55×); total deltas 126.4 → 69.0 MB (0.55×). Second measured anchor at **1.5 m** (255,778 probes, 132,610 valid, 52%): id 41 92.8 → 44.3 MB (0.48×); total deltas 193.2 → 90.7 MB (0.47×). The ratio *strengthens* toward shipping density — finer grid → lower valid fraction → more invalid probes dropped — so the 2 m 0.55× is conservative. Record 1.0 m figures when a clean full-density compile is available (the 1.5 m run's own compile crashed at the unrelated shadowmask-atlas stage; the `--sh-analyze` JSON completed first).
- [ ] At-rest delta VRAM from the once-per-load `ComposeStorageFootprint` (dev-tools), cross-checked against compiler-reported payloads.
- [ ] Per-frame indirect and direct compose GPU time via `POSTRETRO_GPU_TIMING=1`, adapter and GPU class named. On a host lacking `TIMESTAMP_QUERY`, a recorded handoff, not a completion gate.
- [ ] Valid-probe fraction and per-section entry counts as emitted (anchor: stress 67,838 / 115,200 valid).
- [ ] The cap-headroom consequence stated as a consequence: entries/lights that fit under the fixed 256 MiB cap before vs after compaction (a ratio, framed per Direction — ceiling raised, not "lights selected").

## Tasks

### Task 1: format contracts — id 27/41/45 versions + per-cell valid descriptor (level-format)

Lock the on-disk contracts everything downstream compiles against. For `crates/level-format/src/delta_sh_volumes.rs` (id 27) and its siblings `direct_sh_delta_volumes.rs` (id 41) and `animated_direct_sh_delta_volumes.rs` (id 45): bump each section-internal version. Replace the fixed per-entry payload identity (`affinity_lights.len() × PROBES_PER_CELL × stride`, asserted in `to_bytes`/`from_bytes` and mirrored in `direct_sh_delta_has_valid_csr_shape`, `crates/level-compiler/src/pack.rs:216-221`) with a per-entry identity summed over entries: entry `e`'s payload length is `valid_count(cell(e)) × stride`, where `valid_count(cell)` is the popcount of that cell's stored valid-probe descriptor. (Naming, reconciled once: `FORMAT_PROBES_PER_CELL` is the bake-site name for the same 64 as `PROBES_PER_CELL`; `stride` == `tile_stride` == `delta_probe_f16_stride`.) For **id 45** specifically, this per-entry length identity is centralized in `AnimatedDirectShDeltaVolumesSection::validate_wire_contract` (`animated_direct_sh_delta_volumes.rs:104`), reached by both `try_to_bytes`/`to_bytes` and `from_bytes` — a hard `Result`-returning site, unlike id 27/id 41's two body-local `debug_assert`s; make the id-45 edit there. `from_bytes` also independently recomputes `subblock_count = list_len × PROBES_PER_CELL × probe_f16_stride` in its own body to size the byte-read loop — that arithmetic must ALSO move to variable-length parsing (descriptor parsed first, subblock_count summed per-cell). `from_bytes`'s parse-sizing computation is a second, co-edit site for id 45; `validate_wire_contract` is not the single site. Add the descriptor to the wire layout: **one 64-bit mask per affinity cell** (`affinity_cell_count` masks), carried in the section — the carrier is **resolved** to a self-validating per-section mask (not derive-and-cross-check), self-describing so the section validates its own payload length standalone. It must answer `is_valid(local)`, `within_cell_rank(local)`, and `valid_count(cell)`, and must let the loader cross-check against id-34 validity. Parse validates: known version, descriptor present and well-formed, per-entry payload length identity, CSR monotonicity and coverage unchanged, empty-cell/absence encoding unchanged. All version rejects use the existing named recompile-error shape (`from_bytes` version guard); no compat shim (`development_guide.md §1.6`). Within-cell storage order stays x-fastest, filtered to valid probes — pin that as the one canonical order the compaction pass writes and the compose reads. Round-trip and reject tests per section, including a fully-valid cell (identical to today's 64-probe layout), a mixed-validity cell, and an all-invalid cell (zero stored tiles) fixture.

**Cross-workspace constructor blast radius.** Adding the per-cell descriptor field to the three delta section structs breaks every existing struct-literal construction site across the workspace — level-compiler bakes (`delta_sh_bake.rs`, `direct_sh_bake.rs`, `animated_direct_sh_bake.rs`), `render-cpu/src/sh_compose.rs`, renderer `sh_volume.rs`, and test fixtures (~60 sites). This task must also update every such constructor to populate the new field, emitting an **all-valid (dense) mask** — Task 2's compaction pass is what later shrinks it; at Phase 1 the mask is dense so the tree compiles and Task 1's own reject tests build. Same-change rule: update all call sites in the same change that adds the field (`development_guide.md`).

### Task 2: id 41 thin slice — bake dense-64 + compaction + direct compose + parity (level-format + level-compiler + render-cpu + renderer + WGSL)

The narrowest vertical path through every seam, on the dominant section, to falsify the within-cell-rank resolution, the base↔delta cross-check, and the f16-parity assumption before fanning out. **Bake** (`crates/level-compiler/src/direct_sh_bake.rs`): unchanged — keeps emitting dense-64 tiles per retained entry (`per_entry_payload_bytes`, `direct_sh_bake.rs:388`, stays 64-based on the bake output). **Compaction pass** (new `apply_valid_probe_compaction`, `crates/level-compiler/src/delta_sections.rs`; owner of the valid-probe rework): consumes the post-drop id-41 section, stores each retained CSR entry's tiles for valid probes only, in x-fastest within-cell order, driven by id-34 probe validity as the single source of truth; emits the per-cell descriptor; computes the compacted per-entry payload and stats byte totals off the per-cell valid count instead of `FORMAT_PROBES_PER_CELL`. `delta_sections.rs` has no handle to id-34 today (`PostBakeDeltaSections` / `apply_exact_zero_drop_policy` take no validity argument): this task threads the id-34 `OctahedralShVolume` section — available at the construction seam, `pipeline.rs:1051` — plus the affinity-cell↔global-probe mapping into `apply_valid_probe_compaction`, so the pass can build each cell's 64-bit mask. Naming this plumbing as this task's work, reused unchanged by Tasks 3–4. Folding in the cap tail-call: `enforce_payload_cap` is invoked today as the tail call of `apply_exact_zero_drop_policy` (`delta_sections.rs:119`); inserting `apply_valid_probe_compaction` between drop and cap requires lifting that call out of `apply_exact_zero_drop_policy` (or invoking compaction before it inside that fn) — this task makes that edit. Replace the hardcoded 64-probe length check in `direct_sh_delta_has_valid_csr_shape` (`pack.rs:216-221`) with the summed per-cell identity over the compacted section; `direct_sh_delta_covers_selection` and the dims/tile/border checks in `direct_sh_delta_is_usable_for_selection` (`pack.rs:224`) are validity-agnostic and carry over unchanged. **render-cpu** (`crates/render-cpu/src/sh_compose.rs`): `build_direct_delta_buffers` / `DirectDeltaComposeBuffers` carry the per-cell descriptor and the CPU-precomputed per-entry offset array (prefix-summed over `valid_count(cell)` per entry from the post-drop CSR + descriptor, in entry order == payload write order); `ComposeStorageFootprint` counts the compacted `delta_subblocks`. The id-34 cross-check (below) must run **before** this builder sizes/fills the compacted buffer or derives the offset array (the buffer build happens between parse and dispatch). **renderer** (`crates/renderer/src/render/direct_sh_compose.rs`, Renderer-owns-GPU): the descriptor and entry-offset array are packed into ONE combined storage buffer — add its wgpu creation, one new BGL entry in the direct-compose bind group layout, and the matching bind-group entry; name it `BIND_DELTA_COMPACTION_META` at the next-free index in this layout (28 — 27 is `BIND_DEBUG_OVERRIDE`). **Compose** (`crates/renderer/src/shaders/direct_sh_compose.wgsl`): add a descriptor-driven `if (!is_valid(local_probe)) { continue; }` **before** `read_delta_texel` (`:108-116`) — a NEW load-bearing step: this pass binds no per-probe indirection and has no binding-26 early-out, so without the skip an invalid local's `within_cell_rank` aliases a neighbor's slot or reads a zero-length entry OOB. `read_delta_texel` (today `(entry * PROBES_PER_CELL + local_probe) * stride`) then resolves the base as `offset[entry]` (the precomputed array) and the in-entry slot by `within_cell_rank(local_probe)`. The composed direct atlas keeps its dense geometry and the dispatch/gate structure (`direct_compose_should_dispatch`) is unchanged. **Loader:** wire the base↔delta id-34 cross-check and the id-41 all-or-nothing reject clearing the id 40 `EntityShadowLights` + id 41 `DirectShDeltaVolumes` + id 42 `ShadowmaskAtlas` set (compacted buffers never built). **Parity — two pinned oracles, not one.** (a) A pure-CPU **resolver-arithmetic** unit test validating `offset[entry] + within_cell_rank(local) × stride` resolution and the descriptor-popcount payload-length identity against a dense-64 model — CI-unconditional, but tests the resolver model, not the shipped WGSL. (b) A **GPU-dispatch-and-readback** parity test composing the real `direct_sh_compose.wgsl` both ways (dense-64 reference vs compacted) and asserting f16-bit-identity **at valid-probe texels** on `campaign-test`, under a nonzero **promotion** weight (`selection_weights`; at-rest all-zero never runs the id-41 read) — SELF-SKIPS on a headless/no-adapter host (precedent: `curve_eval_test.rs`; `testing_guide.md §3` notes headless CI is the norm). This slice pins the descriptor carrier, the resolver, the cross-check, and both oracles that Tasks 3–4 reuse.

**Tests must cover** (inlined from the Ordering-pins table; that table is not handed to task agents):
- D2 — mixed-validity cell (k valid): compaction stores k tiles x-fastest over valid; compose maps each valid local to its rank, skips invalid; composed atlas f16-identical at valid-probe texels.
- D3 — direct compose, in-range probe whose local is invalid: descriptor `is_valid(local)` skip runs before `read_delta_texel`; no read; f16-identical at valid texels; this NEW skip is required because the direct pass has no binding-26-style early-out.
- D5 — a retained zero-length entry (highest-canonical-zero in id 41) in an all-invalid-for-this-light cell, nonzero weight: compose resolves `offset[entry]` with `valid_count 0`; reads nothing; no OOB into the next entry or past the buffer; parity holds.
- D6 — entry k has `valid_count 0`, entries k+1… follow: entry-offset array prefix-summed over `valid_count(cell)` per entry from the post-drop CSR + descriptor, in entry order; `base(k+1) == base(k)`; offsets never carried across the drop renumber.
- D7 — delta descriptor for cell C self-consistent but its probe SET disagrees with id-34: cross-check runs before buffer sizing / offset-array derivation; all-or-nothing reject clearing the id-41 selection set (40+41+42); compacted buffers never built; no wgpu crash.
- D10 — exact-zero entry + nonzero entry in a partially-invalid cell, cap below compacted need: bake(dense-64) → exact-zero drop (walks dense-64) → compaction pass → cap on compacted bytes; zero entry dropped, survivor at valid_count(C) tiles; named hard error listing ids 27/41/45 and the overage; nothing emitted.
- D11 — pre-bump `.prl` (old 64-probe section) loaded by the new runtime: rejected on version with the named recompile error; no compat shim.

### Task 3: id 27 — indirect delta compaction + compose (level-compiler + render-cpu + renderer + WGSL)

Reuse the Task 2 substrate for the indirect delta. **Bake** (`crates/level-compiler/src/delta_sh_bake.rs`): unchanged, keeps dense-64. **Compaction pass** (`apply_valid_probe_compaction`): store valid-probes-only per entry (id-34 validity), emit the per-cell descriptor; move the compacted length identities off `PROBES_PER_CELL` (`delta_sh_bake.rs:199,585` and the local-slot math at `:636`). **Compose** (`crates/renderer/src/shaders/sh_compose.wgsl`): the indirect compose already early-outs invalid probes via the base `probe_indirection` word (binding 26) before the delta loop, so only valid probes reach `read_delta_texel` (`:169-177`) — this is the ONE path where "invalid already never read on the composed path" holds natively (Task 2/4's new skip is the direct/animated analogue; do not remove this early-out). Change `read_delta_texel` indexing to `offset[entry] + within_cell_rank(local_probe) × stride` from Task 2. Reuse boundary: binding 26 gives the base **global** slot for the base-atlas fetch and the invalid early-out; it does **not** give within-cell rank, so the per-cell descriptor from Task 1 still drives the delta read. **renderer** (`crates/renderer/src/render/sh_compose.rs`): add the combined descriptor+entry-offset storage buffer, one new BGL entry, and the matching bind-group entry — `BIND_DELTA_COMPACTION_META` at the next-free index in this layout (27 — 26 is `BIND_PROBE_INDIRECTION`). This is the pass's last free storage-buffer slot: 7→8 compute storage buffers, at the `maxStorageBuffersPerShaderStage = 8` downlevel ceiling; the `compose_layout_keeps_seven_compute_storage_buffers_and_local_sampler` test expectation updates to eight. **render-cpu** indirect delta buffer builder carries the descriptor and offset array. **Loader:** a descriptor↔id-34 disagreement is a **named hard load error, all-or-nothing** (indirect has no selection list to clear). Parity test composes id 27 against the dense-64 oracle on both maps, **full-atlas** identical, at rest and under a nonzero **animation** weight — same two-oracle method as Task 2 (resolver-arithmetic unit test, CI-unconditional; GPU-dispatch-and-readback parity test, self-skips headless).

**Tests must cover:**
- D2 — mixed-validity cell (k valid): compaction stores k tiles x-fastest over valid; compose maps each valid local to its rank, skips invalid; composed atlas f16-identical full-atlas (indirect early-outs invalids via binding 26).
- D6 — entry k has `valid_count 0`, entries k+1… follow: entry-offset array prefix-summed over `valid_count(cell)` per entry from the post-drop CSR + descriptor, in entry order; `base(k+1) == base(k)`; offsets never carried across the drop renumber.
- D7 — delta descriptor for cell C self-consistent but its probe SET disagrees with id-34: cross-check runs before buffer sizing / offset-array derivation; **named hard load error, all-or-nothing** (indirect has no selection list to clear); compacted buffers never built; no wgpu crash.
- D10 — exact-zero entry + nonzero entry in a partially-invalid cell, cap below compacted need: bake(dense-64) → exact-zero drop → compaction pass → cap on compacted bytes; zero entry dropped, survivor at valid_count(C) tiles; named hard error listing ids 27/41/45 and the overage; nothing emitted.
- D11 — pre-bump `.prl` (old 64-probe section) loaded by the new runtime: rejected on version with the named recompile error; no compat shim.

### Task 4: id 45 — animated-direct delta compaction + compose (level-format + level-compiler + render-cpu + renderer + WGSL)

Sibling of Task 2 for the animated-direct delta. **Bake** (`crates/level-compiler/src/animated_direct_sh_bake.rs`): unchanged, keeps dense-64. **Compaction pass** (`apply_valid_probe_compaction`): valid-probes-only per entry, per-cell descriptor; move the compacted stats/length totals off `PROBES_PER_CELL` (`:136,226,375`). The on-disk per-entry length identity is centralized in `AnimatedDirectShDeltaVolumesSection::validate_wire_contract` (`animated_direct_sh_delta_volumes.rs:104`; edited in Task 1) — a hard `Result`-returning site, unlike id 27/id 41's two body-local `debug_assert`s — plus the co-edit site in `from_bytes`'s own `subblock_count` parse-sizing computation (Task 1). **Compose** (`crates/renderer/src/shaders/animated_direct_sh_compose.wgsl`): add the descriptor-driven `if (!is_valid(local_probe)) { continue; }` **before** `read_delta_texel` (`:130`) — a NEW load-bearing step (this pass, like direct, binds no per-probe indirection and has no early-out); then resolve via `offset[entry] + within_cell_rank(local_probe) × stride`. Preserve the id-45 optional-section contract (an empty post-drop section is absent, `delta_sections.rs:116`). **renderer** (`crates/renderer/src/render/animated_direct_sh_compose.rs`): add the combined descriptor+entry-offset storage buffer, one new BGL entry, and the matching bind-group entry — `BIND_DELTA_COMPACTION_META` at the next-free index in this layout (27 — 26 is `BIND_ANIMATED_DEBUG_OVERRIDE`). **render-cpu** animated-direct buffer builder carries the descriptor and offset array. **Loader:** a descriptor↔id-34 disagreement is a **named hard load error, all-or-nothing**. Parity test composes id 45 against the dense-64 oracle at **valid-probe texels**, at rest and under a nonzero **animation** weight, on both maps — same two-oracle method as Task 2 (resolver-arithmetic unit test, CI-unconditional; GPU-dispatch-and-readback parity test, self-skips headless).

**Tests must cover:**
- D2 — mixed-validity cell (k valid): compaction stores k tiles x-fastest over valid; compose maps each valid local to its rank, skips invalid; composed atlas f16-identical at valid-probe texels.
- D3 — animated-direct compose, in-range probe whose local is invalid: descriptor `is_valid(local)` skip runs before `read_delta_texel`; no read; f16-identical at valid texels; this NEW skip is required because the animated-direct pass has no binding-26-style early-out.
- D5 — a retained zero-length entry (script-mutable slot in id 45) in an all-invalid-for-this-light cell, nonzero weight: compose resolves `offset[entry]` with `valid_count 0`; reads nothing; no OOB into the next entry or past the buffer; parity holds.
- D6 — entry k has `valid_count 0`, entries k+1… follow: entry-offset array prefix-summed over `valid_count(cell)` per entry from the post-drop CSR + descriptor, in entry order; `base(k+1) == base(k)`; offsets never carried across the drop renumber.
- D7 — delta descriptor for cell C self-consistent but its probe SET disagrees with id-34: cross-check runs before buffer sizing / offset-array derivation; **named hard load error, all-or-nothing**; compacted buffers never built; no wgpu crash.
- D12 — id 45 empty after drop + compaction: compaction normalizes to section absent (existing optional contract), not an empty-CSR header.

### Task 5: measure, verify, findings (findings note)

Compile and run both maps end to end. Record every measured-finding AC in a sibling `findings.md` as before→after with ratio: per-section and total bytes (from `--sh-analyze` `(a)compacted` cross-checked against emitted sizes); at-rest delta VRAM from `ComposeStorageFootprint`; indirect and direct compose GPU time via `POSTRETRO_GPU_TIMING=1` with the adapter and GPU class named; valid-probe fraction and per-section entry counts; the cap-headroom ratio framed per Direction. Confirm the honesty-gate ACs (parity against the dense-64 oracle × both maps — full-atlas for id 27, valid-probe-texels for id 41/45; the invalid-direct-texel-safety gate — source-guard CI-unconditional + GPU-readback dev/local; version rejects; base↔delta cross-check reject — hard load error for id 27/45, selection clear for id 41; cap fail-loud with a below-need fixture; boot with unchanged consumer-stage budgets). Anchor map: `content/dev/maps/stress-warren-showcase.map` is confirmed present (research.md §Measured basis); boot-check it exists before pinning the measurement anchor. No per-frame log noise; one info summary per bake/compaction stage; once-per-load footprint under dev-tools only.

## Sequencing

**Phase 1 (sequential):** Task 1 — the on-disk contracts everything downstream compiles against.
**Phase 2 (sequential, thin slice):** Task 2 — id 41 end-to-end (bake → compose → parity); falsifies the within-cell-rank resolver, the base↔delta cross-check, and the f16-parity method on the dominant section before fan-out.
**Phase 3 (concurrent):** Task 3, Task 4 — id 27 and id 45 reuse Task 2's proven substrate, including the `apply_valid_probe_compaction` pass and the entry-offset-array pattern built in Task 2 (each supplies its own section-specific wiring). Independent shaders and format modules. **Shared-file caveat:** both touch `crates/render-cpu/src/sh_compose.rs` and the shared `apply_valid_probe_compaction`/`delta_sections.rs` path; if those modules are not cleanly split per section, run them sequentially instead.
**Phase 4 (sequential):** Task 5 — observes the integrated system; consumes all three sections.

## Invariants

`/orchestrate` hands this table to every task agent with the Goal and its task text. Task paragraphs reference rows, never restate them.

| Invariant | Established by | Preserved / threatened at | Verified by |
|---|---|---|---|
| C1 — Lossless: compaction drops only invalid probes (zero composed signal) and reorders nothing a reader sees; the id 27 indirect composed atlas is **full-atlas** f16-bit-identical to the uncompacted result, the id 41/45 direct composed atlases are f16-bit-identical **at valid-probe texels** (invalid direct texels become `base`, safe only because the sampler zero-weights them — gated by the invalid-direct-texel-safety AC, not assumed) | Task 1 (membership rule), compaction pass + Tasks 2–4 (pass writes only valid; compose reads only valid, with the direct/animated `is_valid` skip) | Every compaction/compose edit | Parity gate vs dense-64 oracle (both maps, at rest + nonzero weight); invalid-direct-texel-safety gate (source-guard CI-unconditional + GPU-readback dev/local) |
| C2 — id-34 validity is the single source of truth: the **compaction pass** stores exactly the probes id-34 marks valid, in x-fastest within-cell order; the loader cross-checks each delta descriptor against id-34 (before buffer sizing/offset derivation) and rejects mismatch all-or-nothing — a named hard load error for id 27/45, the selection-set clear (id 40 + 41 + 42) for id 41 | Task 1 (format cross-check), Task 2 (loader wiring) | Tasks 2–4 emission; the id-41 selection posture | Self-consistency gate, cross-check reject gate |
| C3 — Within-cell resolution, not global rank: the delta read resolves `(cell, local_probe) → stored slot | absent` via the per-cell descriptor (`within_cell_rank`) plus the CPU-precomputed per-entry offset array (`offset[entry]`), never via the base global-slot word (binding 26), which is the wrong rank space | Task 1 (descriptor), Task 2 (resolver) | Tasks 3, 4 (reuse the resolver) | Parity gate, self-consistency gate |
| C4 — Compose-internal only: composed atlas geometry, sampler bind groups, `probe_tile_origin`, and **consumer/sampler-stage** binding budgets (forward, fog, billboard, skinned, kinematic) are unchanged; each of the three compose passes legitimately gains exactly one combined descriptor+offset storage buffer (`BIND_DELTA_COMPACTION_META`), with the indirect pass landing at the 8-buffer downlevel ceiling; compaction never reaches a sampler | Existing sampler/atlas contracts | Tasks 2–4 (must not touch samplers or composed-atlas geometry) | No-budget-regression gate, boot |
| C5 — Cap + exact-zero drop: exact-zero drop runs first and **unchanged on dense-64**; the compaction pass runs after it; the hard-error cap counts **compacted subblock bytes only** (descriptor excluded); over-cap stays the named error naming all three ids; no silent degrade; the dropped-entry set is identical whether the drop sees dense-64 or compacted input | Task 2 (compaction pass + cap accounting), preserved Tasks 3–4 | `delta_sections.rs` drop→compaction→cap path | Cap gate (below-need fixture) |
| C6 — f16 at rest / f32 arithmetic: payloads stay `Rgba16Float`, read via `unpack2x16float`; no WGSL `enable f16` | Existing compose paths | Tasks 2–4 | Code review, parity gate |
| C7 — Version discipline: every touched section rejects non-current versions with the named recompile error; no compat shim | Task 1 | Any later format edit | Version-reject gate |

## Ordering pins

Each row is a `(scenario, ordering, expected outcome)` a test can assert. Tasks 2–4 tests and Task 5 cover these.

| # | Scenario | Ordering | Expected outcome |
|---|---|---|---|
| D1 | Cell C: all 64 probes valid | Compaction stores 64 tiles; compose resolves rank = local | Byte-identical layout and composed result to the pre-compaction 64-probe path |
| D2 | Cell C: mixed validity (k valid) | Compaction stores k tiles x-fastest over valid; compose maps each valid local to its rank, skips invalid | Composed atlas f16-identical to uncompacted (full-atlas id 27, valid-texels id 41/45); indirect early-outs invalids via binding 26, direct/animated skip via the descriptor (see D3) |
| D3 | Direct/animated compose, in-range probe whose local is **invalid** | Descriptor `is_valid(local)` skip **before** `read_delta_texel`; no read | No read; f16-identical at valid texels. Corrects D2's "already never read": that holds natively only for the indirect (binding-26) path; direct/animated need this NEW skip or the invalid local aliases a neighbor's slot / reads a zero-length entry OOB |
| D4 | Cell C: all 64 invalid (solid interior) | Compaction stores 0 tiles for entries in C; descriptor popcount 0 | Entry payload length 0; compose reads nothing for C; no mean-over-∅; parity holds (uncompacted stored dead zeros there too) |
| D5 | Retained **zero-length** entry (script-mutable slot in id 27/45; highest-canonical-zero in id 41) in an all-invalid-for-this-light cell, nonzero weight | Compose resolves `offset[entry]` with `valid_count 0` | Reads nothing; no OOB into the next entry or past the buffer; parity holds. (D4 covers the all-dropped case; this covers a retained zero-length survivor) |
| D6 | Entry k has `valid_count 0`; entries k+1… follow | Entry-offset array prefix-summed over `valid_count(cell)` per entry from the **post-drop** CSR + descriptor, in entry order | `base(k+1) == base(k)`; offsets never carried across the drop renumber; tail stays aligned |
| D7 | Delta descriptor for cell C **self-consistent** but its probe SET disagrees with id-34 for C | Cross-check runs **before** buffer sizing / offset-array derivation | All-or-nothing reject; id 41 selection set (40+41+42) cleared, id 27/45 named hard load error; compacted buffers never built; no wgpu crash; never reaches compose |
| D8 | Mismatch on an earlier CSR entry than the one read | Load cross-check precedes any compose | One wrong entry never silently misaligns the tail — the D7 load cross-check rejects first |
| D9 | Exact-zero entry in cell C, plus a nonzero entry in C | Exact-zero drop (dense-64) removes the zero entry first; compaction shrinks the survivor | Zero entry absent from CSR (unchanged spelling); survivor stored at valid_count(C) tiles; parity holds |
| D10 | Exact-zero entry + nonzero entry in a **partially-invalid** cell, cap below compacted need | bake(dense-64) → exact-zero drop (walks dense-64) → compaction pass (separate) → cap on compacted bytes | Zero entry dropped; survivor at valid_count(C) tiles; named hard error listing ids 27/41/45 and the overage; non-zero exit; nothing emitted |
| D11 | Pre-bump `.prl` (old 64-probe section) loaded by the new runtime | Loader parses section header | Rejected on version with the named recompile error (C7); no compat shim |
| D12 | id 45 empty after drop + compaction | Compaction normalizes | Section absent (existing optional contract), not an empty-CSR header |

## Open questions

- **Cap-selection policy (owner decision).** The current cap is a hard error with no light selection; the task framing described a "cap that fits by dropping whole lights," which the shipped code does not do (`enforce_payload_cap` errors; `apply_exact_zero_drop_policy` drops only exact-zero entries). Compaction raises the ceiling that compiles under a fixed cap but does not add selection. Does the owner want a future drop-to-fit-by-selection policy (so a map over cap degrades gracefully instead of failing)? Out of scope here; it would change the quality-vs-bandwidth framing materially. Recorded, not resolved.
- **Storage-buffer headroom, spent.** Packing the descriptor and entry-offset array into one combined `BIND_DELTA_COMPACTION_META` buffer per compose pass keeps the indirect pass within the 8-per-stage downlevel ceiling, but lands it there with zero headroom (7→8). The density-coarsening successor must account for this: it will find no free storage-buffer slot on the indirect compose pass.
- **Descriptor sharing across the three sections.** Validity is a property of the base grid, identical for ids 27/41/45. Whether the three carry independent copies (simplest, self-describing) or share one derived-at-load structure is an implementation choice within the fail-loud constraint; the spec does not pin it. (The carrier itself is resolved — a self-validating per-section 64-bit-per-cell mask, carried, not derived-nothing; see Direction and Task 1. Only the cross-section sharing stays open.)
</content>
</invoke>
