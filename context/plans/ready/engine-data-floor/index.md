# Engine-Data Floor Extraction

> **Status:** ready — reviewed (3 structural rounds → converged; implementability pass applied). Capture in `context/lib/scripting.md` §12.
> **Related:** `context/lib/scripting.md` §12 · `context/lib/build_pipeline.md` · `context/lib/development_guide.md` · `context/plans/drafts/compile-time-reduction/` · `context/plans/drafts/scripting-core-extraction/` (rebases onto this floor: its Task 2 is superseded, Task 3 reshaped) · sibling `research.md`

## Goal

Extract the VM-free engine-data + evaluation substrate out of the `postretro` binary crate into a layered crate floor, so the entity/scripting data model stops recompiling as part of the engine binary and gives combat (Epic 16), enemy behavior, and networking a clean dependency base. Behavior-preserving: no runtime, wire-format, scripting-semantics, or SDK-typedef change. This is the foundation the scripting-core (VM runtime) extraction and handler relocation then build on.

## Why a re-draft

`scripting-core-extraction` assumed `scripting/components/` was a clean VM-free leaf movable into one crate. It is not: components couple to `movement`/`nav`/`weapon`/`ai` and to the IR substrate, and `ComponentValue` embeds every component **by value**, so the registry transitively pulls those couplings. The floor is larger and **layered**. See `research.md` for the source-grounded dependency map and the seven cycles this plan breaks.

## Scope

### In scope

- Extract a lower **foundation** crate (`postretro-foundation`): the IR evaluator core, the `MovementScope`/`PlayerMovementComponent` cluster, the **foundation-clean** POD descriptor *types* (movement/weapon/health/ai/light), the POD value types (`Vec3Lit`/`EulerDegrees`), and the sunk subsystem PODs (`DamagePayload`, `NavAgentParams`, `ModMapEntry`).
- Extract an upper **entities** crate (`postretro-entities`): the entity registry, `ComponentValue`, the remaining components, `ScriptCtx`, the slot table + engine-state catalog, provenance, the system-command queue, the descriptor types that reference an entities-resident type — the **reaction/crossing descriptors** (reference `EntityId`), **`MeshDescriptor`** (references `AnimationState`/`InterruptPolicy`, which live in the mesh component), and **`EntityTypeDescriptor`** (aggregates `BillboardEmitterComponent` and the mesh state enums) — plus `DataRegistry`. Depends on the foundation.
- **Descriptor partition rule (corrected — round-2 finding):** a descriptor type lives in the **foundation** only if *every type it references* is foundation-resident (no `EntityId`, no entities component, no entities descriptor). "`EntityId`-free" is **necessary but not sufficient** — a descriptor can be `EntityId`-free yet still embed an entities-resident type (e.g. `EntityTypeDescriptor` → `BillboardEmitterComponent`; `MeshDescriptor` → `AnimationState`). Any such descriptor lives in **entities**. The implementer verifies each descriptor by chasing its referenced types transitively, not just its own fields.
- Break the seven cross-crate cycles in place first (behavior-preserving): split `data_descriptors` types from VM converters; relocate `Vec3Lit`/`EulerDegrees` out of VM-coupled `conv.rs`; sink the subsystem PODs; carve `ir/scopes.rs` from the IR core. (Cycles where a descriptor aggregates an entities-resident type — `SequenceStep`→`EntityId`, `EntityTypeDescriptor`→`BillboardEmitterComponent`, `MeshDescriptor`→`AnimationState` — are broken by *placement* via the partition rule, not by in-place moves.)
- Invert `movement`/`nav`/`weapon`/`ai` (and the opaque-handle consumers — crates that store or forward `EntityId`/floor descriptors without inspecting them) to depend *up* on the floor crates.
- Per-crate orphan-rule FFI: each floor crate owns its types' marshalling impls behind an optional `script-ffi` feature, off by default. No crate enables it unconditionally; the runtime enables `postretro-entities/script-ffi`, which forwards to `postretro-foundation/script-ffi`.

### Out of scope

- Extracting the VM runtime crate (`scripting-core`) — the rebased `scripting-core-extraction` follow-on owns that.
- Relocating primitive/reaction handlers to subsystems (the A1 work) — also the follow-on.
- Refactoring GPU-free `render::ui` descriptor/layout/style types into a CPU-only model crate — that is `compile-time-reduction` Task 6. (Deciding that the UI-coupled manifest types `RegisteredUiTree`/`LevelManifest` stay out of the floor *is* in scope — Task 2 — but this plan does not touch their internals or the `render::ui` types they embed.)
- PRL-loader / visibility extraction (`compile-time-reduction` Tasks 3–5).
- `inventory`/`linkme`. Any change to primitive names, wire shapes, marshalling results, SDK typedef output, PRL format, or runtime behavior.

## Acceptance criteria

- [ ] The `postretro-foundation` and `postretro-entities` crates exist as workspace members; `cargo build --workspace` and `cargo test --workspace` pass.
- [ ] `cargo tree -p postretro-foundation` and `cargo tree -p postretro-entities` (each resolving its own feature graph; default features) show **no** `rquickjs`, `mlua`, `wgpu`, `winit`, `glyphon`, or `kira`; with `--features script-ffi` each adds `rquickjs`/`mlua` only. (Manual/CI gate, not a `#[test]`.)
- [ ] The dependency graph is acyclic and one-way: `postretro-entities` → `postretro-foundation`; the consumers (`movement`/`nav`/`weapon`/`ai`, `netcode`, `agent_steering`, `sim`, `startup`) depend on the floor crates, never the reverse. `cargo build --workspace` proves no cycle. (`audio`/`collision`/`render` import no floor types in production — no flip.)
- [ ] Editing a floor crate and running *its* tests does not recompile `rquickjs`, `mlua`, `wgpu`, `glyphon`, `kira`, or `winit`.
- [ ] `RegisteredUiTree`/`LevelManifest` and the `data_descriptors` VM converters remain in `postretro`; no floor crate depends on `render::ui` or the VMs by default.
- [ ] Placement holds: `DamagePayload`/`NavAgentParams`/`ModMapEntry` and the foundation-clean descriptors (movement/weapon/health/ai/light) + the pure numeric/IR validators resolve from `postretro-foundation`; the reaction/crossing descriptors, `MeshDescriptor`, `EntityTypeDescriptor`, `build_crossing`, `apply_damage`/`attach_agent`, and `EntityId` resolve from `postretro-entities`; the `mlua`/`render::ui` validators (`validate_dense_lua_array`, `parse_*`) and `js_err`/`lua_err` stay in `postretro`; `weapon`/`nav` define neither sunk POD; `MenuCamera`/`Frontend` stay in `postretro`.
- [ ] `DataRegistry`'s public API names no `LevelManifest`/`RegisteredUiTree`/`render::ui` type — manifest population is via POD-typed setters, so `postretro-entities` compiles with no `render::ui` dependency.
- [ ] The SDK typedef drift test (`scripting/typedef/tests/committed.rs`, run via `cargo test -p postretro`) passes — generated `postretro.d.ts`/`postretro.d.luau` byte-identical to pre-refactor. This requires the migrated `gen-script-types` bin (Task 4) to produce identical output after its `#[path]` re-includes become crate dependencies.
- [ ] All existing scripting/movement/nav tests pass unchanged from their relocated homes (and any existing combat tests still pass as a regression check); cross-runtime parity tests still pass.
- [ ] No `unsafe` added (pre-existing `unsafe` in moved code travels; the gate is net-new only). No `wgpu` call moves out of the renderer. No PRL/wire/semantics change.
- [ ] Each extraction PR quotes before/after timings from the baseline (Task 1) for the targeted edit loops.

## Tasks

### Task 1: Baseline (gate)

Capture the baseline fresh unless a prior one is committed in this plan folder (`research.md` or a `baseline.md`) — do not rely on locating another orchestration's output. Capture: warm `cargo check -p postretro`; a touch-rebuild of `crates/postretro/src/scripting/registry.rs`; and `cargo build -p postretro --timings` noting where `rquickjs-sys`/`mlua-sys`/`luau0-src` and the `postretro` self-compile sit on the critical path. All later tasks quote against this.

### Task 2: Break the cycles in place (behavior-preserving, no crate yet)

Stage the structural moves inside `postretro` before any crate boundary, so the extractions in Tasks 3–4 are clean module moves rather than refactors-under-transplant. Each sub-step preserves the public surface (re-export from the old path) so call sites keep compiling. **`data_descriptors` purity is at the *file* level, not just the directory** — the `types/` dir already exists but every file reaches its imports via `use super::*`/`use super::super::*` against the VM+`render::ui` glob in `mod.rs`, and two files mix down-bound and up-bound types. So splitting means per-file import surgery, not just moving directories:

- **Split `data_descriptors` types from converters.** The VM converters (`data_descriptors/{js,lua}/`) and the `mod.rs` VM/`render::ui` glob stay on the runtime side. For each descended `types/*.rs` (and `error.rs`/`validate.rs`), **replace the `use super::*` glob with explicit, VM-free imports** — that is the load-bearing edit; the dir already existing does not mean the split is done.
- **Split `validate.rs` three ways** (round-2 finding — it is not "pure vs mlua"). Carve into: **foundation** — the genuinely pure numeric/IR validators (`validate_at`, `validate_positive_finite`/`_non_negative_finite`/`_in_range_finite`/`_exclusive_min`, `validate_finite_f32`/`_array3`, `validate_primitive_name`, `is_catalog_path_relative_to_content_root`, and `validate_dash_expr`/`ir_node_from_json`/`ir_type_label`, which use only `MovementScope`+IR — both foundation); **entities** — `build_crossing` (it constructs `CrossingDescriptor`/`CrossingCondition`, which are entities-resident); **runtime** — the `mlua::Table` validators (`validate_dense_lua_array`) and the `render::ui` enum-string parsers (`parse_anchor`/`parse_align`/`parse_capture_mode`/`parse_easing`/`parse_focus_kind`/`parse_role`/`parse_priority`). The pure set does not call the mlua/UI helpers, so it descends cleanly. **Classify every remaining `validate.rs` fn by the same rule** — references only foundation types (numeric/IR/`DescriptorError`) → foundation; constructs an entities descriptor → entities; touches `mlua`/`render::ui` → runtime. In particular check `validate_interactive_name`/`validate_image_name` against source (reviewers split on whether they touch `render::ui`): pure string checks → foundation, name-resolution against UI types → runtime. The runtime and entities validators may still call foundation's `DescriptorError` (a one-way down-edge — do not duplicate the error type).
- **Split `data_descriptors/error.rs`.** Only `DescriptorError` (`EntityId`/VM-free) descends to the foundation; its VM adapters `js_err(rquickjs::Error)`/`lua_err(mlua::Error)` stay runtime-side (or under `script-ffi`).
- **Split `types/manifest.rs` and re-plumb `populate_from_manifest` (real surgery, not a rename).** `ModThemeTokens`/`ModFontAssets` are POD and descend; `RegisteredUiTree`/`LevelManifest` embed `render::ui::AnchoredTree` and **stay runtime-side**. `LevelManifest` embeds the reaction/crossing descriptors (→`postretro-entities`) plus `RegisteredUiTree` (runtime), so `LevelManifest` itself stays runtime-side. Today `DataRegistry::populate_from_manifest(&mut self, manifest: LevelManifest, …)` (`data_registry.rs`) names `LevelManifest` and does **composition** (level-tag scoping, `recompose_active_sets`) — that is why this is surgery, not a destructure: (1) the `LevelManifest` destructure + tag-scoping that produces the POD reaction/crossing lists moves to the runtime caller; (2) `DataRegistry` gains POD-typed setters (one per manifest-derived field — `set_level_reactions`/`set_level_crossings`/etc., minus the `RegisteredUiTree`/UI pieces) plus a `recompose` entry, keeping the compose logic on `DataRegistry` but never naming `LevelManifest`; (3) **all `populate_from_manifest` call sites migrate** to the setter form (~6 production — `startup/lifecycle.rs`, `scripting/systems/ui_proxy.rs`, `state_crossings.rs`, `reactions/registry.rs`, `render/ui/style_ranges.rs` — plus ~16 in `reaction_dispatch.rs` tests). Net: `postretro-entities` compiles with no `render::ui` dep.
- **Relocate `Vec3Lit`/`EulerDegrees`** out of VM-coupled `conv.rs` into a VM-free module (`Vec3Lit` is stored by value in `light`/`billboard_emitter`; `EulerDegrees` is FFI-boundary-only). During this in-place stage their `FromJs`/`IntoJs`/`FromLua`/`IntoLua` impls **stay co-located in a module that `postretro` compiles unconditionally** (no real `cfg` yet — `postretro` always has the VMs); the impls become `#[cfg(feature = "script-ffi")]`-gated only when `postretro-foundation` is created in Task 3. Behavior must not change — the FFI impls keep compiling in `postretro` throughout.
- **Sink the subsystem PODs.** Move `DamagePayload` (`weapon/damage.rs`) and `NavAgentParams` (`nav/mod.rs`) into VM-free modules re-exported from their old paths. `ModMapEntry` (`runtime/types.rs`) likewise. **`MenuCamera`/`Frontend` stay in `postretro`** — `DataRegistry` does not store them, so the floor does not need them.
- **Carve `ir/scopes.rs`** so the IR core (`ir/{mod,bind,eval,scope,load}.rs`) has no dependency on `ScriptCtx`/`slot_table`/`primitives`; `StoreScope` stays runtime-side. This also edits `ir/mod.rs` (the `mod scopes;` declaration and the `StoreScope`/`StoreCapability` re-export move runtime-side). One `#[cfg(test)]` snag: `ir/eval.rs`'s tests import `scopes::{StubScope, StubWrite}` — add a **foundation-local** stub `BindingScope` impl in the IR core's test module (the trait is foundation-resident) so `eval.rs`'s tests stay with `eval` in the foundation rather than shipping to the runtime crate.
- **`DataRegistry` storage is already POD.** Its *stored fields* (entities/maps/reactions/crossings + `ModMapEntry`) are POD and it never stores `ui_trees` (it destructures them away). So no field-removal is needed — the only `DataRegistry` work is the `populate_from_manifest` API rework above. Confirm no stored field type pulls `render::ui` or the VMs.

Build green after each sub-step. The genuinely disjoint sub-steps (the `DamagePayload`/`NavAgentParams`/`ModMapEntry` sinks, the `ir/scopes.rs` carve) may fan out; the `data_descriptors` sub-steps are ordered — the `validate.rs`/`manifest.rs` splits and the `DataRegistry` verification follow the types/converters split, since they reference paths it churns.

### Task 3: Extract `postretro-foundation`

Move the cluster from Task 2 into the new lower crate: IR core, `MovementScope` + `PlayerMovementComponent` + `MovementState`/`DashPrograms`, the **foundation-clean** descriptor types (movement/weapon/health/ai/light) + `DescriptorError` + the pure numeric/IR validators carved in Task 2, `Vec3Lit`/`EulerDegrees`, and the sunk PODs (`DamagePayload`/`NavAgentParams`/`ModMapEntry`). The reaction/crossing descriptors, `MeshDescriptor`, `EntityTypeDescriptor`, and `build_crossing` do **not** go here — they reference entities-resident types and belong in `postretro-entities` (Task 4). Default deps: `glam`, `serde`, `serde_json`, `thiserror`, `log`. Add an optional `script-ffi` feature (`dep:rquickjs`, `dep:mlua`) compiling the FFI impls for the foundation's own types. Mirror `crates/level-format/Cargo.toml`. `movement`/`nav`/`weapon` flip to depend on it: re-point their `use crate::scripting::…` imports to the foundation crate. `movement/` is the involved case (the `movement`↔`scripting` mutual edge resolves here) — `movement/{mod,substrate,scope,intents,dispatch}.rs` import `PlayerMovementComponent`/`MovementState`/`MovementScope`, which now resolve from the foundation; `nav`/`weapon` re-point `NavAgentParams`/`DamagePayload`. Widen every moved symbol referenced across the new crate boundary from `pub(crate)` to `pub`. (Keep each type's *current* field visibility — do not tighten as part of the move; e.g. `Vec3Lit` is `struct Vec3Lit(pub [f32; 3])` today and its by-value constructors in the FFI module + tests rely on that, so the tuple field stays `pub`. `EntityId`'s field stays private — see Task 4.) Exit check: `cargo build --workspace` stays green (this catches a cycle introduced by the `movement`/`nav`/`weapon` flips at this phase, not only at Task 5's terminal gate). Quote before/after edit-loop timings against the Task 1 baseline in the PR.

### Task 4: Extract `postretro-entities`

Move `registry.rs`, the remaining `components/*` + their `&EntityRegistry` behavior fns (`apply_damage`, `attach_agent`, mesh/brain — note `apply_damage`/`attach_agent` now import `DamagePayload`/`NavAgentParams` from `postretro-foundation`, sunk in earlier phases), `ctx.rs`, `slot_table.rs` + `engine_state_catalog.rs`, `provenance.rs`, `scripting/error.rs` (`ScriptError` — **not** `data_descriptors/error.rs`, which is `DescriptorError` and descended in Task 3), `reactions/system_commands.rs` (queue + command enum), the **reaction/crossing descriptor types** (`SequenceStep`/`NamedReaction`/`ReactionDescriptor`/`PrimitiveDescriptor`/`ProgressDescriptor`/`CrossingCondition`/`CrossingDescriptor` — embed `EntityId`) plus the `build_crossing` validator, **`MeshDescriptor`** and **`EntityTypeDescriptor`** (they reference entities-resident `AnimationState`/`BillboardEmitterComponent`), and `DataRegistry` into the new upper crate. **Cargo wiring (load-bearing):** depend on `postretro-foundation` with `default-features = false`, and make this crate's own `script-ffi` feature *forward* to the foundation's — `script-ffi = ["dep:rquickjs", "dep:mlua", "postretro-foundation/script-ffi"]`. Do **not** enable the foundation's `script-ffi` unconditionally, or a default `cargo tree` of this crate pulls the VMs and violates the isolation AC. This crate's `script-ffi` compiles the FFI impls for `EntityId`/`Transform`/`ComponentKind`/`ComponentValue`/components/reaction descriptors. `EntityId` stays an opaque handle to non-scripting modules (private field; `to_raw`/`from_raw` widen to `pub` for the runtime crate's `conv.rs` to reach). Widen moved `pub(crate)` symbols to `pub` as in Task 3. **Migrate the `gen-script-types` bin** (`crates/postretro/src/bin/gen_script_types.rs`): it currently recompiles the scripting tree a second time via `#[path = "../scripting/mod.rs"]` + hand-built shim modules for `weapon`/`nav`/`movement`/`render::ui`. With the types now in crates, convert those `#[path]` re-includes to real `use postretro_foundation::*` / `use postretro_entities::*` dependencies, and confirm the bin still builds and emits byte-identical `postretro.d.ts`/`postretro.d.luau` (this bin backs the typedef-drift AC). Exit check: `cargo build --workspace` stays green. Quote before/after timings against the Task 1 baseline in the PR.

### Task 5: Consumer flips + verification

**Productive work:** Tasks 3–4 already flip the subsystems whose dependency they create (`movement`/`nav`/`weapon` for the sunk types; the `postretro-entities` consumers). Task 5 flips the *remaining* consumers — the verified set (grep-confirmed against source): **`netcode/`** (~10 files, the largest), **`agent_steering.rs`**, **`scripting/systems/ai.rs`**, **`sim/`** (`mod.rs`, `host_movement.rs`, `predict_reconcile.rs` — import `apply_damage`/`EntityId`/`ComponentKind`/`EntityRegistry`/`PlayerMovementComponent`/`Transform`), and **`startup/lifecycle.rs`** (`ComponentKind`/`ComponentValue` + billboard component) — re-pointing their `use crate::scripting::…` imports to the floor crates. **Not** `audio/` or `collision/` (zero scripting imports) and **not** `render/` (its only scripting deps are `slot_table::SlotValue` and `RegisteredUiTree`, both of which stay in `postretro` — no flip). These are import-path updates (the consumers treat the types as opaque handles / data), low-conflict and per-subsystem, so they fan out.

**Verification gate:** confirm no reverse edge (`cargo build --workspace` proves acyclic); run the isolation `cargo tree` checks and the touch-rebuild timing checks from the AC; verify the typedef drift test is byte-identical.

## Sequencing

**Phase 1 (sequential):** Task 1 — baseline gates the timing claims.
**Phase 2 (sequential):** Task 2 — the in-place cycle-breaking is the foundation; its sub-steps fan out internally but the phase gates the extractions.
**Phase 3 (sequential):** Task 3 — `postretro-foundation` is the dependency floor; `postretro-entities` can't compile against it until it exists.
**Phase 4 (sequential):** Task 4 — `postretro-entities` depends on Task 3's `postretro-foundation` (and its `script-ffi` feature).
**Phase 5:** Task 5 — the consumer-flip sweep (ai + opaque-handle consumers) fans out concurrently across subsystems once both crates exist; the verification gate (`cargo build --workspace` acyclic, isolation `cargo tree`, typedef drift) runs once, after all flips land.

Structural surgery (Phases 2–4) is serialized deliberately; the flip fan-out is Phase 5, with verification as its terminal gate.

## Rough sketch

**Crate layout (two layers, one-way edge).**
- `crates/foundation/` (`postretro-foundation`): IR evaluator core + movement/IR cluster + foundation-clean descriptor & value PODs + sunk subsystem PODs. Default = no VMs. `script-ffi = ["dep:rquickjs","dep:mlua"]` compiles its types' FFI impls.
- `crates/entities/` (`postretro-entities`): registry + `ComponentValue` + remaining components + `ScriptCtx` + registries + `DataRegistry` + the entities-bound descriptors (reaction/crossing, `MeshDescriptor`, `EntityTypeDescriptor`). Depends on `postretro-foundation` with `default-features = false`; its `script-ffi` forwards to the foundation's (`script-ffi = ["dep:rquickjs","dep:mlua","postretro-foundation/script-ffi"]`) so a default `cargo tree` of this crate stays VM-free.

**Why layered, not one crate.** The `{IR core + MovementScope + PlayerMovementComponent + descriptor PODs}` cluster has no edge up into `EntityId`/`EntityRegistry`/`ComponentValue` (research.md §3) — a clean one-way edge. One crate would force the foundation and the registry to recompile together for no structural reason; both grow under Epic 16 (combat adds descriptors and IR adoption; the registry grows components). Splitting lets each recompile independently. (This `postretro-foundation` → `postretro-entities` layering mirrors Unreal's `Core` → `CoreUObject` and Bevy's `bevy_reflect` → `bevy_ecs`.)

**Orphan-rule, per crate.** Each crate owns the marshalling impls for the types it defines, behind its own optional `script-ffi` feature pulling the VMs — `impl rquickjs::FromJs for LocalType` is legal only where the type is local. Default builds have no VM deps; the runtime crate enables both features. Foreign types (glam) wrap in local newtypes (`Vec3Lit`/`EulerDegrees`). This is `scripting.md §12`'s contract, applied to two crates.

**What stays in `postretro` (the future runtime crate).** `ir/scopes.rs` (`StoreScope`), `conv.rs` FFI/json bridges, `data_descriptors/{js,lua}/` converters + the `render::ui` glob, `RegisteredUiTree`/`LevelManifest`, `runtime/*`, `luau.rs`/`quickjs.rs`/`primitives/*`/`primitives_registry.rs`/`reaction_dispatch.rs`/typedef generator.

**Relationship to `scripting-core-extraction`.** That spec's Task 2 (extract `entity-core`) is replaced by this plan. Its Task 3 (extract `scripting-core`) is reshaped to pull the VM-coupled remainder above and enable `script-ffi` on both floor crates. Its Tasks 4–6 (ScriptingCore sub-struct, test relocation, handler relocation) ride unchanged on top. After this lands, rebase that spec onto the floor before re-orchestrating it.

## Decisions

- **Two-layer floor** (`postretro-foundation` + `postretro-entities`), not one crate — the cluster's one-way edge makes it clean and lets the foundation and registry recompile independently as both grow.
- **Descriptor partition by transitive reference (corrected round 2).** A descriptor lives in `postretro-foundation` only if *every type it references* is foundation-resident; if it references any entities-resident type (`EntityId`, a component, or a state enum), it lives in `postretro-entities` with the registry. So the reaction/crossing descriptors (→`EntityId`), `EntityTypeDescriptor` (→`BillboardEmitterComponent`), and `MeshDescriptor` (→`AnimationState`) all live in entities. This breaks the aggregation cycles by placement rather than by sinking `EntityId`/components down (which would dissolve the layering — `EntityId` and the components stay the entities crate's). "`EntityId`-free" alone is not the test.
- **POD-sink inversion** for the `health→weapon` and `agent→nav` edges: sink the trivial PODs (`DamagePayload`, `NavAgentParams`) into `postretro-foundation`; the behavior fns (`apply_damage`, `attach_agent`) stay with their component. `ModMapEntry` sinks too; `MenuCamera`/`Frontend` do not (the floor doesn't consume them).
- **`data_descriptors` splits at the *file* level** — types (down) from VM converters + UI-manifest types (stay up). `validate.rs` and `types/manifest.rs` are mixed files that must be carved, not moved whole; `LevelManifest` stays runtime-side and `DataRegistry`'s manifest population takes POD pieces. The `render::ui` CPU-only-model cleanup is deferred to `compile-time-reduction` Task 6.
- **`ir/scopes.rs` stays in the runtime crate** (pulls `ScriptCtx`/`slot_table`/`primitives`); only the VM-free IR core descends.
- **Crate names: `postretro-foundation` (lower) + `postretro-entities` (upper).** "Foundation" (not "core") names the base layer in the Apple-Foundation / Unreal-`Core` sense without competing with "the engine is the core"; "entities" names the closed entity/component model without implying an extensible ECS (a stated non-goal). Both match the workspace's `postretro-<role>` convention.

## Open questions

- None. (Scope, partition rule, cycle fixes, Cargo wiring, and names are all resolved.)
