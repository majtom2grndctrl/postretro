[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Development workflow commands for Postretro"
license.workspace = true

[dependencies]
# `cargo metadata` is the only input both remaining jobs read: the crate-graph
# reduction and the target directory a distribution's binaries land in.
serde_json = { workspace = true }
