# Resource Grant Chokepoint (E16)

## Goal

Engine-owned resources can only ever decrease. `apply_damage_with_context` subtracts health; nothing adds to health or ammo. Build the blessed inverse: one validated chokepoint that *adds* to health and ammo, reachable from two entry points — an impact-policy effect crediting the damage **source**, and a tag/activators-targeted reaction primitive. Ships the reference dev-mod policy a mod replaces wholesale: grant-ammo-on-kill plus a trigger-fed ammo pickup, so a playtest that empties its reserve is no longer a dead end.

**This is also where an impact effect first addresses an entity other than the one that was hit.** Every command effect today applies to the dispatch's target; grant is attacker-directed, so the planned command must carry which token it addresses and resolve the source. That widening is the least reversible thing in this spec and the stated prerequisite of `E16--per-player-currency` — it is a first-class goal here, not a side effect of Task 2.

## Prerequisites

- **`E16--impact-policy-substrate`** (shipped) — the impact dispatch source, the closed impact-effect set this adds two members to, the `@impact.source` command-target token (published, no methods today), the evaluator's evaluate-then-apply model, and the consequential/presentation arm split.
- **`E16--impact-death-lifecycle`** (shipped) — the kill latch, pending kill credit, and the `set_health_absolute` resurrect re-arm this spec's health grant flows through.
- **`E16--ammo-resource`** (shipped) — `AmmoReserve` and its `available`/`take`/`credit` interface, and the `player.ammo` / `player.ammoReserve` owner-private HUD projection that makes a grant observable.

## Scope

### In scope

- **One grant chokepoint** owning validation, delegation, and outcome reporting for both entry points, so a pickup cannot bypass a rule a kill-grant respects. The `[0, max]` clamp stays owned by `set_health_absolute`, which the health grant delegates to.
- **Source-addressed effect application** — a planned command carries the token it addresses, and the apply path resolves the source rather than assuming the target.
- **Health grant** — additive, routed through the existing absolute-write chokepoint so it inherits both the health-range clamp and the resurrect re-arm.
- **Ammo grant** — additive credit to a named reserve pool on the recipient, saturating.
- **Impact-effect arms** — `grantHealth` / `grantAmmo` on the `SourceHandle`, the sixth and seventh members of the closed impact-effect set at the wire/SDK level (`despawn` / `setHealth` / `setState` / `playAnim` / `slot.add`; three dedicated `ImpactEffect` variants plus two that lower through the generic `BoundEffect::Write` path), targeting `@impact.source` only. `Effect` itself is an opaque brand, not a union: adding a member is four type edits, two arms each: the SDK's `ImpactEffectWire` in `sdk/lib/data_script.ts`, its hand-maintained twin union in `sdk/lib/data_script.luau`, `BoundEffect`, and `ImpactEffect` — since `setState` and `slot.add` lower through `BoundEffect::Write` to `PlannedEffect::Write` rather than to `ImpactEffect`.
- **Reaction primitives** — `grantHealth` / `grantAmmo`, tag-targeted and activators-targeted, siblings of `applyDamage`.
- **Reference dev-mod policy** — grant-ammo-on-kill in the dev mod's mod-global lifecycle, plus a trigger-fed ammo pickup in the combat demo map.

### Out of scope

- **Per-player mod currency (XP, score, credits).** Grant covers engine-owned resources only. Mod currency is a store write, and a *per-player* one needs per-owner slot cardinality keyed by a player identity that outlives the level — `E16--per-player-currency`, which sequences after this spec and is parked pending the session work that owns that identity. Team-shared currency already works today via `slot.add`. The roadmap item names xp alongside ammo and health for the source-crediting grant, so deferring the currency half is a deliberate narrowing of that item, not an omission.
- **Damage numbers.** The closed effect set has no path to presentation, and the `combat presentation substrate` roadmap item owns that display layer. This spec's own roadmap item (`resource-grant chokepoint + dev-mod reference`) additionally lists armor, xp, and a reference damage-number policy as part of this chokepoint; all three deferrals belong in one amendment.
- **Loot drops.** Spawning a pickup entity on death is a spawn effect, not a grant. Belongs with the `pickup` roadmap item (Weapon Systems). This chokepoint is what such an item calls into when walked over.
- **Armor.** No armor component exists anywhere in the engine; it belongs to the Damage & Defenses milestone. The impact-effect set stays open to an armor grant — a third grant verb, the set's eighth member.
- **Per-type ammo carry caps.** Decided, not deferred by omission — see Decisions.
- **A declared-ammo-type registry.** The type stays a free-form charset-validated identifier, as the ammo spec pinned.
- **Client-side prediction of grants.** Grants are host-authoritative and reach the owner through the shipped owner-private slot projection on the next snapshot.

## Direction

**Problem.** Engine resources are write-once-downward. The only ammo in the world is what a weapon descriptor seeds at spawn, so a playtest that empties its reserve cannot recover, and no reward policy can pay out in anything the engine owns. The cause is a missing chokepoint, not a missing policy: `combat-events.md` §7 named the resource-grant path as "the one genuinely new engine mechanism" the economy needs, and three shipped specs each deferred to it by name.

**Prior commitments.**
- *Closed-but-extensible effects.* `E16--impact-policy-substrate` Decisions pinned that a later spec needing a new effect "adds an arm to the set," naming "the economy epic's `grant`" as the case. This is that spec; it adds arms rather than widening the union to a hollow type.
- *Resources engine-owned, currencies mod-owned.* `combat-events.md` §2 — health/ammo are engine-owned and readonly to scripts; granting flows through a blessed chokepoint, never a store write. Honored.
- *`applyDamage`'s dual shape.* The damage path is both an in-tick chokepoint and a tag-targeted reaction primitive. Grant mirrors it exactly, including the negative/non-finite warn-and-no-op precedent.
- *The reserve's write side was parked here.* `E16--ammo-resource` Decisions: "'Takes up space' is a write-side concern enforced when ammo *enters* the reserve — the deferred grant/pickup chokepoint." This spec owns that seam and decides it (Decisions: carry cap).
- **Divergence.** `combat-events.md` §5 sketches `grant("player.health", 25)` — a resource-name string. Not adopted; see Alternatives. That research doc's `defineCombatHandler` / `onKill` surface is superseded wholesale by the impact substrate: "grant on kill" is an impact policy gated on the kill edge, not a kill event. The doc's §2 principles and §6 attribution model still hold.

**Placement.** The chokepoint lives in the entities crate beside the nouns it writes (`set_health_absolute`, `AmmoReserve::credit`), not inside the impact evaluator. This is not a dependency-direction argument — both callers live in the `postretro` binary crate, so a binary-side module would compile fine. It is precedent and symmetry: the damage chokepoint this inverts already lives in the entities crate with the components, and that precedent, not a rule in `scripting.md` §13, is what carries the argument: §13's handler-placement paragraph co-locates a handler's pure logic *and* its wiring with the owning subsystem, which neither mandates nor forbids the split used here. Grant logic beside the components, grant wiring beside the evaluator and the reaction registry, mirrors the damage path exactly.

**Alternatives rejected.**
- *One `grant` arm carrying a typed closed resource selector* — `grant(ammo("bullets"), 8)` over a `{ Health, Ammo { type } }` union, rather than one verb per resource. **This is the real fork, and it is the shape decision that is expensive to reverse.** It has a strong precedent one layer down: the weapon descriptor's `resource` field is already a serde-tagged union keyed by kind, with heat and cell as planned siblings, and making "what you grant" and "what a weapon consumes" one vocabulary is a property a later carry cap would want. Rejected on three grounds. First, the growth premise is weaker than it looks. Heat and cell are roadmap-committed (`roadmap.md`, Weapon Systems: "the other two union variants plus the per-tick resource update"), but neither is grantable, and their own definitions in `research/weapon-model.md` §3 are why. Heat is a cost you shed — passive dissipation plus an overheat gate — so the reward that touches it is a *vent*, which is subtraction, and this spec already routes subtraction to its own verb. Cell is per-instance charge with no reserve and no reload that passively regenerates after `regenDelayMs`, so crediting it duplicates the regen the weapon already runs every tick. Both are per-weapon-*instance* and self-updating; neither is a pawn-held pool a reward credits. Armor is the only genuine third grantable resource — one added arm, not three. The fork reopens only if a resource becomes pawn-held *and* externally credited; heat and cell becoming union variants does not do it. Second, every sibling in the closed effect union is a verb (`despawn`, `setHealth`, `playAnim`); a selector reintroduces a "which noun" parameter into a vocabulary that deliberately has none, and the union's whole point is that a policy cannot name an effect the evaluator does not implement. Third, health carries no pool key and ammo requires one, so a shared selector holds a field meaningless for one variant — a runtime validity rule where the type system could carry it. Reversal cost is bounded and known: the project is pre-stable and the SDK surface is generated, so if heat, cell, and armor all turn out to be grantable, collapsing the verbs into a selector is mechanical with call sites updated in the same pass. **Settled, not deferred.** The "choose before the SDK publishes" deadline is real but does not bind, precisely because the project is pre-stable and `scripting.md` §1 makes the trigger explicit: breadth grows with demand, not ahead of it. A selector today is a catalog of points along an axis that has one occupant.
- *A string-addressed `grant("player.ammo", 20)`* — the `combat-events.md` §5 sketch. Distinct from the typed selector above and weaker than it: the string cannot carry the ammo pool key, and the sketch names `player.ammo` — the read-only owner-private projection of the weapon *magazine*, not the reserve a grant credits, and not a writable slot at all. The string API teaches a false model of where resources live.
- *Grant as a store write with an engine-side readonly bypass.* Cheapest to build, and the write path already exists. Rejected: it collapses the engine-owned/mod-owned boundary the whole economy design rests on, and the HUD slots are projections of components — writing the slot would desync the component that feeds it.
- *Impact-effect arm only, no reaction primitive.* A scope trim rather than a rival shape — a later spec could add the reaction arm with no rework. Rejected because it ships the reward path while leaving pickups and heal stations unbuildable, and because the health case needs it most: the reaction registry today holds exactly one health primitive, `applyDamage`, so a medkit volume is unbuildable at any level of authoring effort. Fan-out is the other half — the IR has no iteration, so granting several players in one fire is only expressible through tag targeting.
- *Absolute-write reaction primitives (`setHealth` / `setAmmo`) instead of additive grants*, with policies expressing rewards as absolute writes over IR arithmetic. Rejected, and the reason argues for the chosen shape: a tag-targeted fan-out has no per-target read, so "heal everyone in this volume by 25" cannot be written as an absolute value. Additive is the correct primitive once the recipient is a set.

## Decisions

- **One chokepoint, two entry points.** Both entry points call the same pair of functions. Validation, delegation, and logging live there once; neither caller re-implements a rule.
- **The chokepoint owns every warn, unconditionally.** Invalid-amount and missing-component warnings are emitted once, inside the grant functions, as bare `log::warn!` matching `applyDamage`'s wording and level. The outcome enum is returned for control flow only; neither entry point logs on top of it. **No rate limiting in v1** — the impact arm fires per hit, so a mistargeted grant will spam, and that is the intended dev-build signal: these are authoring errors to fix, not noise to suppress. A limiter is machinery ahead of demand, and adding one later is a change *inside* the chokepoint with no call-site churn — the same seam this spec banks for the carry cap. It also stays buildable: the only rate-limited warn in the engine (`rate_limited_warn`, emitter bridge) is binary-crate-side and unreachable from `postretro-entities`, so a limiter here would mean relocating shared logging machinery for a problem no playtest has reported.
- **Health grant routes through the existing absolute-write chokepoint** — read current, add, write via `set_health_absolute`, which already clamps to `[0, max]` and, on a positive stored result, clears `death_handled`, the pending kill credit, and the live contributor ledger. **This is deliberate, not incidental:** healing a downed entity above zero must re-arm kill detection and discard the credit from the down it recovered from, which is exactly what an authored `setHealth` resurrect already does. One health-write chokepoint, not two. A grant that leaves the entity at zero (amount `0`, or a dead entity at zero max) re-arms nothing.
- **Ammo grant credits the named pool, saturating.** The reserve is `u32`; a granted amount is truncated toward zero. Saturation at the integer ceiling is the existing `credit` behavior and is preserved. An amount that truncates to `0` returns applied and credits nothing, without a warn; an amount above `u32::MAX` saturates to `u32::MAX` before `credit`.
- **No per-type carry cap in v1.** A cap needs an authoring surface for the per-type limit, and none of the three plausible homes exists yet (the weapon descriptor's type block is per-weapon not per-pawn; there is no declared-ammo-type registry; the inventory that will own the reserve is unbuilt). Granting is unbounded up to saturation. The *seam* is what this spec banks: every path by which ammo enters a reserve **after spawn** goes through one function. Descriptor spawn seeding (`seed_weapon_reserve` in `crates/postretro/src/scripting/builtins/data_archetype.rs`) keeps its own direct `AmmoReserve::credit` call and stays deliberately uncapped — a weapon descriptor declares what it spawns with, and clamping that to a pawn-level cap is a separate decision. A later cap is therefore a two-site change: the grant chokepoint, and an explicit choice about whether spawn seeding respects it.
- **Amounts: IR expression on the impact arm, plain number on the reaction arm.** The impact arm takes a `NumberValue`, so "grant a quarter of the overkill" is expressible. The reaction arm takes a finite `f32` — **this is forced, not stylistic**: `E18--dispatch-scope-params` pins that the only IR-valued descriptor arg positions are the `setState` value and `accumulate`, and a runtime value anywhere else is rejected rather than evaluated. Both arms validate identically at the chokepoint.
- **Negative and non-finite amounts warn and no-op.** Mirrors `applyDamage`'s precedent. The rationale is health-shaped and should not be over-read: for health, a negative grant would be a damage path bypassing the contributor ledger, the impact dispatch, and the kill latch — every attribution guarantee in the epic. Ammo has no ledger, no dispatch, and no latch, so the honest rule there is simply that **subtraction gets its own verb**. A later spend/drain/disarm effect is deferred, not foreclosed, and it should not read as violating an invariant this spec wrote. On the impact arm a non-finite amount cannot reach the chokepoint: non-finite IR arithmetic resolves to zero upstream and authored literals must be finite, so that path applies a zero-amount no-op. Non-finite rejection is exercised at the chokepoint and on the reaction arm, where a non-finite literal is rejected by descriptor validation.
- **The impact arm targets `@impact.source` and nothing else.** Grant is attacker-directed by definition, and a policy naming `@impact.target` fails to bind with a named diagnostic, exactly as a mistargeted `setState` does today. **The cost is asymmetric and worth stating plainly.** For health nothing is lost: healing the entity that was hit is already expressible as `setHealth(healthAfter.plus(n))`, an absolute write with additive arithmetic. For ammo there is no analogue — no absolute ammo write exists anywhere in the engine — so a co-op support weapon that resupplies a teammate you shoot is inexpressible after this spec. Accepted for v1, and the genre is why rather than the schedule: co-op *is* a committed direction (`index.md` §4 puts authoritative client-server co-op in scope as Epic 15), so "not yet multiplayer" would be a weak reason. But resupply-by-shooting-a-teammate is a class-based-shooter idiom, not a Doom/Quake one, and co-op in a boomer shooter means shared-world PvE rather than support roles. The co-op cases that do fit the genre — a supply cache both players walk into — are already expressible on the reaction arm. What is foreclosed is narrowly the support-weapon mechanic, and the fix stays cheap and additive (permit the target token on the ammo arm) if a mod wants it.
- **A missing or wrong-shaped recipient is a skip, not a bind failure.** The dispatch's source is optional — a script `applyDamage` reaction carries none, and a weapon fire whose wielder does not resolve carries none — and a policy cannot know at bind time which fires will have one. Enemy melee is a positive case: it sets `DamageContext.attacker`, so a grant on an enemy-melee impact credits the attacking enemy. So: no source → skip the effect; a source the registry no longer resolves (despawned, id recycled) → skip; a source lacking the component the grant writes (an enemy has health but no reserve) → skip with a warn. Every other effect in the same fire still applies. This mirrors `applyDamage`'s per-target skip-and-warn.
- **No ammo-type *registry* check; the same charset check the descriptor runs.** Two different questions, and the spec answers them differently. *Does this pool exist?* — unanswerable, there is no declared registry, and granting a pool no equipped weapon uses is legitimate (stocking shells before the shotgun exists). Deferred to the "declare-your-categoricals → codegen" spec the ammo spec named; a typo still creates a dead pool. *Is this string a well-formed pool key?* — answerable now, and the engine already answers it: `validate_ascii_identifier` gates `AmmoResource.ammo_type` at descriptor validation. The grant arms reuse it. Without that, the write path could create a key no weapon descriptor is permitted to spell — a **guaranteed**-dead pool rather than a probably-dead one, and a key space where the engine's own floor disagrees with itself. Both arms carry the type as an authored literal, so this validates at load with a named diagnostic, not at the chokepoint.
- **The impact arm inherits the producer gate; the reaction arm does not.** App-drain impacts (DoT, environmental) run no policy in v1, so a grant on the impact arm never fires for them. A trigger-fired grant always runs. Both stated in the authoring docs so neither is discovered by surprise.
- **Grant is consequential, not presentation.** It dispatches in the consequential arm of the per-fire loop, before `playAnim`.
- **The grant reads live component state at apply time, never the fire's snapshot.** This is forced by the scope, not chosen: `scripting.md` §2's pre-effect snapshot is *target*-scoped — it freezes the facts the authored expression reads about the entity that was hit — and the grant recipient is the **source**. No snapshot of the source exists to read. So grants within one fire compose additively, and a `setHealth` in the same fire still evaluates its operand from the snapshot and, applying in effect order, overwrites any earlier health grant. Same rule as `setHealth`'s own clamp, which reads live max health at apply time.
- **Attribution stays at `source`; no contributor leaf is exposed.** "Grant to the killer" works. "Split the reward among contributors" does not: the ledger's pre-reduced scalars (`damageBy(source)`, `topContributorShare`) are not IR-readable, and the impact scope publishes four number facts. Deferring costs nothing, which is what makes this a decision rather than a fork — the scope fact tables are append-only (`scripting.md` §11), so a later spec adds a contributor leaf without re-pointing a single bound program. `combat-events.md` §6's demand-gated expressiveness fork is where it lands if a mod asks.
- **Concurrency.** The health grant *is* a read-modify-write, but a single-writer one: only the host writes the component, so there is no race. This differs from `slot.add`, which lowers to a self-referential IR add on a store slot with documented last-writer-wins. Two reward paths, two concurrency stories — worth knowing when a policy mixes them.

## Acceptance criteria

- [ ] An impact policy gated on the kill edge that grants ammo raises the killer's reserve for the named pool; with that pool equal to the equipped weapon's ammo type, the `player.ammoReserve` HUD readout reflects it on the next snapshot. (Ammo values above 2^24 are not exact through the `f32` slot projection; saturation tests must use the reserve component directly.)
- [ ] A trigger-fired grant raises the reserve of every activator that entered the volume, and a tag-targeted grant raises it for every tagged entity.
- [ ] A grant of a negative amount changes nothing and logs a warning containing `"[Grant]"` — through both entry points, asserted binary-side via `log_capture`.
- [ ] A non-finite reaction-arm literal is rejected by the new per-primitive args validation in both VM converters, with a diagnostic naming the field; a non-finite impact-arm IR result resolves to zero upstream and applies a silent no-op. Non-finite rejection inside the chokepoint is covered by unit test.
- [ ] A health grant that would exceed maximum health stores maximum health, not more.
- [ ] A health grant lifting a latched zero-HP entity above zero re-arms kill detection: the entity can be killed again and reports exactly one further kill. A grant that leaves it at zero preserves the latch and its pending credit.
- [ ] A health grant healing a living entity above zero clears the contributor ledger; a subsequent kill credits only post-heal damage — asserted in the entities-crate unit test.
- [ ] An in-tick impact whose source is absent — an unresolved weapon wielder, constructed via `apply_damage_with_context` with `DamageContext { attacker: None, producer: InTick }` — applies no grant and does not panic; other effects in the same fire still apply. (A script `applyDamage` reaction is *not* this case: it is `AppDrain`, so it runs no policy at all — see the app-drain criterion.)
- [ ] A grant whose recipient lacks the written component is skipped with a warning; sibling effects in the same fire still apply.
- [ ] A policy that aims a grant at the impact target fails to bind with a diagnostic naming the event, and every other registered policy still loads and runs.
- [ ] App-drain-sourced impacts run no grant, while a reaction-fired grant on the same frame applies normally.
- [ ] A unit test on the ammo grant asserts that a credit taking the reserve past `u32::MAX` stores `u32::MAX` rather than wrapping. Saturation is not verified through the `player.ammoReserve` projection, which is an `f32` slot exact only through 2^24.
- [ ] Manual walkthrough gate on the combat demo map: killing a `dummy`-tagged target raises the reserve on each resurrect-branch down, and walking into the pickup volume raises it again — a player who empties the reserve can recover without restarting.
- [ ] Review gate: the impact-effect grant arms are exposed on `SourceHandle` and not on the target handle, and the reaction builders are exposed as `data_script` free functions. `tsc --noEmit -p content/dev/scripts/tsconfig.json` introduces no *new* errors beyond the pre-existing `reactive-ui-fixture.ts` TS2322 (`tsc` is not wired into xtask or CI, so this is manual). The runnable gate is the existing `committed_sdk_types_match_current_registry` drift test.
- [ ] A tag-targeted `grantHealth` raises health on every tagged entity carrying health in one fire, and a trigger-fired `grantHealth` on `on.activators` heals every activator.
- [ ] A policy group containing both a grant and a `playAnim` applies the grant before the animation switch within the same fire.
- [ ] An impact-arm grant whose amount is an IR expression over impact facts credits the evaluated value; a `RuntimeValue` in a reaction-arm amount is rejected by the new per-primitive args validation (which requires `args.amount` to be a finite JSON number, not an object) rather than evaluated.
- [ ] Review gate: `docs/scripting-reference.md` documents both grant entry points and the producer-gate asymmetry; `context/lib/scripting.md` gains a §10.8 listing `grantHealth` / `grantAmmo` as the reaction registry's additive-resource path, §2's impact-effect inventory carries the two new members, and §11's same-entity write claim is narrowed to per-entity state fields. `context/plans/roadmap.md`'s grant item no longer claims armor, xp, or damage numbers ship here, and `context/research/combat-events.md` carries a header note marking its §5 API sketch superseded.
- [ ] A source-scan test walks the workspace and asserts exactly two non-`#[cfg(test)]` call sites of `AmmoReserve::credit` — the grant chokepoint and `seed_weapon_reserve` — so a third write path fails `cargo test` rather than silently defeating a later carry cap.
- [ ] A fractional ammo grant credits the floor of the amount, and an amount below `1.0` credits nothing while reporting the applied outcome — asserted in the entities-crate unit test. That it logs no warning is asserted binary-side, where `log_capture` is reachable.
- [ ] An impact-arm grant whose amount binds to a non-number root fails to bind with the same diagnostic *shape* `setHealth` uses — naming the grant primitive and its `amount` operand, not echoing `setHealth` — and every other registered policy still loads.
- [ ] A tag-targeted `grantAmmo` over a set where one entity lacks a reserve credits every other entity in the set and warns for the skipped one.
- [ ] Two `grantHealth` effects in one fire compose additively rather than the second overwriting the first, and a `setHealth` in the same fire applies in effect order over an earlier grant.
- [ ] An ammo grant naming a pool key outside `validate_ascii_identifier`'s charset fails at load with a diagnostic naming the field path, on both arms — a per-policy skip on the impact arm, a whole-manifest `DescriptorError` on the reaction arm. A well-formed key naming a pool no weapon uses loads and credits normally.

## Tasks

### Task 1: Grant chokepoint

Add `crates/entities/src/components/grant.rs`, beside the components it writes, owning two functions: `grant_health(registry: &mut EntityRegistry, recipient: EntityId, amount: f32) -> GrantOutcome` and `grant_ammo(registry: &mut EntityRegistry, recipient: EntityId, ammo_type: &str, amount: f32) -> GrantOutcome`, over `enum GrantOutcome { Applied, SkippedNoComponent, SkippedInvalidAmount }` so callers branch without re-deriving the reason. Both reject strictly-negative and non-finite amounts before touching state. **Zero is valid**: it yields `Applied` with no state change — only strictly negative and non-finite are `SkippedInvalidAmount`. Pin the two warn strings, since Task 2 and Task 3 assert against them and the chokepoint is entry-point-agnostic (it can carry neither `applyDamage`'s `[Scripting]` prefix nor its primitive name): `"[Grant] {primitive}: entity {id} has no {component}; skipping"` and `"[Grant] {primitive}: amount {amount} is negative or non-finite; no-op"`. Each function hardcodes its own name as `{primitive}` — `grant_health` emits `"grantHealth"`, `grant_ammo` emits `"grantAmmo"` — so the template is spec-level shorthand, not a runtime parameter. `crates/postretro/src/health/reactions.rs` is the structural precedent for both. Warns are bare `log::warn!` with no rate limiting, and the chokepoint is their sole emitter — entry points branch on the outcome without logging. The pool key arrives already charset-validated at load, so the ammo grant does not re-check it. The health grant pre-checks for a `HealthComponent` and returns `SkippedNoComponent` if absent — `set_health_absolute` silently no-ops on a missing component, so without the pre-check a health-less recipient would wrongly report `Applied`. It then reads the live component value at apply time (never a fire snapshot — the snapshot is target-scoped and the recipient is the source), adds, and writes through `set_health_absolute` (`crates/entities/src/components/health.rs`) — do not clamp or store directly, because that function owns both the `[0, max]` clamp and the positive-result re-arm of `death_handled` / pending kill credit / contributor ledger, and bypassing it would silently drop the re-arm. Note the wider blast radius, which is accepted in v1: `set_health_absolute` clears `death_handled`, the pending kill credit, **and the live contributor ledger** on *any* finite positive stored result, not only on a resurrect — so a grant healing a living, partly-damaged entity resets its damage attribution. Unit-test that behavior deliberately so a future change to it is a visible break. The ammo grant truncates the amount toward zero to an unsigned count and calls `AmmoReserve::credit` (`crates/entities/src/components/ammo_reserve.rs`), which already saturates; a recipient with no reserve component returns the skipped outcome rather than creating one, so a grant cannot conjure a reserve onto an entity that owns no weapon. No carry cap: this function is the sole post-spawn entry point for ammo into a reserve. `seed_weapon_reserve` (`crates/postretro/src/scripting/builtins/data_archetype.rs`) keeps its existing direct `credit` call for descriptor seeding and is not rerouted here. Promote the charset validator both entry points need: `validate_ascii_identifier` is today a private fn in `crates/foundation/src/data_descriptors/types/combat.rs` whose messages hardcode the `` `components.weapon.{field}` `` prefix. Lift it to a `pub fn validate_ascii_identifier(field_path: &str, value: &str)` taking the full field path from the caller, so a grant emits `` `grantAmmo.type` must match [A-Za-z0-9_.:-] `` rather than a weapon-descriptor diagnostic, and update the existing weapon call sites to pass their own path. This lands in Phase 1 because Task 2 and Task 3 both consume it concurrently. Unit-test the clamp, the re-arm hand-off, saturation, the negative/non-finite rejection, and both skip outcomes. Add the caller-count gate: a source-scan test in this module that walks the workspace from `env!("CARGO_MANIFEST_DIR")/../..` and asserts exactly two non-`#[cfg(test)]` call sites of `AmmoReserve::credit` — this function and `seed_weapon_reserve`. Use the masking scan in `crates/postretro/src/scripting/mod.rs` (`mask_comments_and_string_literals`, `collect_boundary_violations`) as the precedent: a naive grep sees eleven `.credit(` occurrences, ten of them inside `#[cfg(test)] mod tests`. The gate fails `cargo test`, not `cargo build`. The warn-capture helper `log_capture` (`crates/postretro/src/scripting/reactions/mod.rs`) is `pub(crate)` to the `postretro` crate and unreachable from `postretro-entities`: assert warn emission from the binary-side entry-point tests in Task 2 and Task 3, and cover only the outcome enum and the state effects in this crate's unit tests.

### Task 2: Impact-effect grant arms

Add the two grant arms to the impact-policy path end to end. **SDK:** add `grantHealth(amount: NumberValue)` and `grantAmmo(type: string, amount: NumberValue)` to `SourceHandle` — today an empty branded interface, the seam this spec was left — both returning the opaque `Effect`. `sdk/types/postretro.d.ts` is generated and must not be hand-edited: declare the methods in the typedef templates (`crates/scripting-core/src/typedef/templates/sdk_lib.d.ts` and `sdk_lib.luau`), implement them on the frozen `IMPACT.source` object in `sdk/lib/data_script.ts` and its Luau twin `sdk/lib/data_script.luau`, re-export the new surface from `sdk/lib/index.ts` (and its Luau twin) beside the existing `damage` / `SourceHandle` exports, or content scripts cannot import it from `"postretro"`; then regenerate `sdk/types/postretro.d.ts` / `.d.luau` via `cargo run -p postretro --bin gen-script-types` and update the committed drift fixtures under `crates/postretro/src/scripting/typedef/tests/fixtures/`; document on each that the recipient is the damager, that a fire with no damager skips the effect, and that app-drain impacts run no policy in v1. Lower both to effect descriptors carrying `"target": "@impact.source"`, matching the existing `target` string channel `bind_effect` already parses. The SDK's shared `impactEffect()` helper hardcodes `target: "@impact.target"` in both `sdk/lib/data_script.ts` and `sdk/lib/data_script.luau`, and the internal `ImpactEffectWire` union pins that literal per arm — add a source-targeted lowering helper and two `target: "@impact.source"` arms to the wire union rather than reusing `impactEffect` as-is. **Bind:** in `crates/postretro/src/impact_policy.rs`, add the two primitives to `bind_effect`; they require `@impact.source` where the four target-bearing shipped arms (`despawn`, `playAnim`, `setHealth`, `setState`) require `@impact.target` and `slot.add` requires the token be absent, so generalize `require_impact_target` into a checker taking the expected token and emitting the same diagnostic shape, rather than adding a second bespoke guard. Only `despawn`, `playAnim`, and `setHealth` call that helper today — `setState` enforces its token through a match guard plus a fallthrough error arm, and `slot.add` through its own `target.is_none()` guard. Leave both as they are; the checker rejects an absent or mismatched `target` on the grant arms only. The shipped test `impact_effect_wire_rejects_raw_store_assignment_and_boolean_operands` loops the four target-bearing arms asserting each rejects `"@impact.source"` — it must keep passing unchanged, so the generalization must not make either token universally acceptable. Bind the amount operand through `bind_read` and reject a non-number root with the same diagnostic *shape* `setHealth` uses — `` grantHealth `amount` must evaluate to a number ``, naming the grant primitive and its own operand rather than echoing `setHealth`'s literal string. The amount's IR leaves stay target/impact-scoped: `bind_read` binds against the target-seeded `EntityScope::impact(...)`, so a `@state.*` leaf inside a source-arm amount reads the *target's* per-entity state. There is no source-scoped fact vocabulary in v1; document that on the SDK methods. The amount lowers through the same number-node path `setState` and `setHealth` use. Run `grantAmmo`'s pool key through Task 1's promoted `validate_ascii_identifier` at bind, passing `"grantAmmo.type"` as the field path, so a malformed key is a load diagnostic rather than a silently dead pool. On this arm the failure is a per-policy skip with a warn — other policies still load — matching `bind_effect`'s existing per-descriptor `continue`. **Apply:** the planned-command path currently applies every command to one id — `apply_planned`, a method on `ImpactPolicyRuntime` in `crates/postretro/src/impact_policy.rs`, takes a `target: EntityId` and hands it to the free function `apply_effect` in `crates/postretro/src/impact_effects.rs`. Widen the planned command to carry which token it addresses — a `CommandRecipient { Target, Source }` discriminant declared in `impact_policy.rs` beside `PlannedEffect` (which lives there, not in `impact_effects.rs`) and populated at bind from the parsed `target` string. Carry it on every `PlannedEffect` variant including `Write`, defaulted to `Target`, so the five shipped arms keep their behavior unchanged and `slot.add` holds an inert `Target` it never resolves. Have `apply_planned` resolve the source id from the dispatch (`ImpactDispatch.source`, an `Option<EntityId>`, already carried) and skip the effect when it is absent or no longer resolves in the registry. Add `GrantHealth` and `GrantAmmo` variants to `ImpactEffect` (the inner enum of `PlannedEffect::Command`), carrying the bound amount and, for ammo, the pool key. Route both new commands to Task 1's functions. Grant is consequential, so classify it with the write/setHealth/despawn arm, not the presentation arm. Document the impact arm's half of `docs/scripting-reference.md`, including the producer-gate asymmetry, in this task; Task 3 owns the reaction half.

### Task 3: Grant reaction primitives

Register `grantHealth` and `grantAmmo` as reaction primitives in a new `crates/postretro/src/grant/` module, not under `health/` — `grantAmmo` writes `AmmoReserve` and has no relationship to the health subsystem, and `scripting.md` §13 co-locates handler wiring with the subsystem it exposes. `crates/postretro/src/health/reactions.rs` remains the wording and structure precedent to mirror. Declare `mod grant;` in `crates/postretro/src/main.rs` alongside `mod health;` — `postretro` is a binary crate with no `lib.rs`. Invoke `register_grant_reactions` from `crates/postretro/src/session/mod.rs`, beside the existing `register_emitter_reaction_primitives` / `register_enemy_state_reaction_primitives` calls. Registrars are invoked explicitly, with no `inventory`/`linkme` collection, so an uninvoked registrar silently registers nothing. Each deserializes typed args (an amount; for ammo, also the pool key), applies the empty-target-set debug no-op that `dispatch` already models, then calls Task 1's functions per target and branches on the returned outcome for control flow only. It emits no warns of its own: the chokepoint owns both the invalid-amount and the missing-component warn, so do not replicate `dispatch`'s dispatch-wide invalid-amount warn here or it will double-log. A skipped target does not abort the rest. Export the SDK builders in `sdk/lib/data_script.ts` and its Luau twin `sdk/lib/data_script.luau`, add `grantHealth` and `grantAmmo` to the selective re-export list in `sdk/lib/index.ts` (and its Luau twin) beside the existing `damage` export, and declare them in `crates/scripting-core/src/typedef/templates/sdk_lib.d.ts` / `sdk_lib.luau`, mirroring `damage(target: ActivatorsTarget | string, amount: number): PrimitiveReactionDescriptor` — the same dual accepting either the `@activators` token or a tag string. The two forms emit different wire keys: the tag form carries `tag`, the token form carries `target: "@activators"`. Args validation needs a new per-primitive arm in **both** VM converters — `crates/scripting-core/src/data_descriptors/js/reactions.rs` and its Luau twin `crates/scripting-core/src/data_descriptors/lua/reactions.rs` — since `PrimitiveDescriptor.args` crosses as free-form `serde_json::Value` with no existing per-primitive seam. Both converters already reject both-present `target` and `tag` generically, so that half is free; neither-present is currently *legal* (absent `tag` means system-targeted), so the new rule is a per-primitive check on the `spawnFromSpawner requires a non-empty tag` model: exactly one of `tag` or `target == "@activators"`. In the same arm, require `args.amount` to be a finite JSON number rather than an object — this is what makes a non-finite literal and a `RuntimeValue` both load-time diagnostics instead of dispatch-time deserialize failures — and run `args.type` through Task 1's promoted `validate_ascii_identifier` with field path `"grantAmmo.type"`. A `DescriptorError` here rejects the whole `setupLevel` manifest, unlike the impact arm's per-policy skip. **The `@activators` form needs trigger-binding work the registry alone does not give you.** Trigger-event reactions do not reach `ReactionPrimitiveRegistry` for entity-targeted effects: `crates/postretro/src/trigger_bindings.rs` classifies each primitive against a hardcoded `CONSEQUENTIAL_PRIMITIVES` list, anything absent is `Presentation`, and `partition_direct_reaction` then drops it with `"[Trigger] sentinel target on non-consequential primitive ... cannot drain app-side; not binding"`. So: add `"grantHealth"` and `"grantAmmo"` to `CONSEQUENTIAL_PRIMITIVES`, add the matching `bind_command` arms in `crates/postretro/src/trigger_bindings.rs`, and add the matching `BoundTriggerCommand` variants, `execute_non_store` arms, and `BoundTriggerCommandKind` entries in `crates/postretro/src/trigger_commands.rs`. The `applyDamage` / `BoundTriggerCommand::Damage` pair is the exact precedent, with one deliberate divergence: `applyDamage` rejects a negative amount *at bind* and never reaches its chokepoint, whereas the grant arms bind regardless of sign and let the chokepoint own the negative warn. Only non-finite is a load-time rejection. The fixed-tick arm calls the same Task 1 functions. Regenerate `sdk/types/postretro.d.{ts,luau}` via `cargo run -p postretro --bin gen-script-types` and update drift fixtures under `crates/postretro/src/scripting/typedef/tests/fixtures/` — when Tasks 2 and 3 land SDK edits in one pass, one regeneration covers both, but the obligation applies to whichever lands last. Without this the tag form works and the `@activators` form silently binds to nothing. A trigger-event reaction can then grant to whoever entered the volume, and a plain reaction can grant to a tagged set. This entry point is not producer-gated and is the only way to grant to several recipients in one fire, since the IR has no iteration. Update `docs/scripting-reference.md` with a grant section covering the reaction entry point. Add `grantHealth` / `grantAmmo` to `context/lib/scripting.md` as the reaction registry's additive-resource path — a new §10.8 rather than an edit to §10.5, since the registrar no longer lives with damage — and leave `applyDamage`'s "(no healing path)" parenthetical intact, as it scopes to that primitive's negative-amount rule, which this spec preserves. Also amend §2's impact-effect inventory to carry the two new members and note that they address `@impact.source`, and qualify §11's "the only entity-scoped write path targets the entity an impact landed on" — that claim now holds for per-entity state fields, not for component writes. Two more doc edits close stale pointers this spec creates. Amend `context/plans/roadmap.md`'s `resource-grant chokepoint + dev-mod reference` item so its "adds to health/ammo/armor" and "reference XP/score/damage-number policy" lines match what actually ships: health and ammo here, xp to `E16--per-player-currency`, damage numbers to `combat presentation substrate`, armor to Damage & Defenses. Add a header note to `context/research/combat-events.md` recording that §5's `defineCombatHandler` / `getCombatEvent` / `onKill` / `grant("player.health", 25)` surface is superseded by the impact substrate and by this spec, while §2's principles and §6's attribution model still hold — three shipped specs cited that doc by name, so a stale API sketch there propagates.

### Task 4: Reference dev-mod policy and pickup

Ship the reference economy a mod replaces wholesale, in the dev mod. Add a grant-ammo-on-kill policy to `content/dev/scripts/combat-lifecycle.ts` beside the existing enemy-death base/override pair, filtered on `{ tag: "dummy" }` — `combat-demo.map`'s four `target_dummy` entities carry `"_tags" "dummy"`, while its single `enemy`-tagged entity is the zombie `combatZombieLifecycle` resurrects rather than removes — registered under the id `"dev:ammo-on-kill"` (`defineImpactEvent` enforces a namespaced `prefix:name` shape and throws at load on an unqualified one), gated on the kill edge (`healthBefore.gt(0).and(healthAfter.le(0))` — a level gate would re-pay on every corpse hit), and calling `impact.source.grantAmmo("bullets.light", 8)` — the reference pistol's pool, declared in `content/dev/scripts/reference-pistol.ts`. Register it in the dev manifest's `events` list in `content/dev/start-script.ts` with `levels` absent, so the reference economy applies to every dev map rather than only the combat demo. Add the trigger-fed pickup: a `trigger_volume` brush in `content/dev/maps/combat-demo.map` (which has none today — `closet-reveal.map` and `spawner-test.map` are the authoring precedents) carrying `"_tags" "ammo_pickup"`, `"activation" "touch"`, `"fire_mode" "multiple"`, a `"rearm_ms"` of `3000`, and `"name" "ammo_pickup_volume"`, with `enabled_on_spawn` left at its default — the compiler's `"trigger_volume ... has neither target_tag, on_fire, nor on_exit; it will be inert"` warning is expected for an `onTriggerEvent`-driven volume and is not a failure — wired to a trigger-event reaction granting 24 `"bullets.light"` to `on.activators`. Register that reaction and its `onTriggerEvent` entry through `setupLevel()`'s returned `reactions` and `triggerEvents` in `content/dev/scripts/combat-demo-reaction.ts`, the level data script `combat-demo.map`'s worldspawn `data_script` KVP already names. That file's `setupLevel` is declared returning `{ reactions: NamedReactionDescriptor[] }` — widen it to include `triggerEvents: TriggerEventDescriptor[]` and import that type, or the file fails `tsc`. The volume is a repeating dispenser in v1 — no self-disarm — since consumable one-shot pickups belong to the `pickup` roadmap item. Keep the two paths visibly distinct in the walkthrough so the demo shows both entry points: kills pay out, and the volume pays out. Update the map's README with the walkthrough, noting that repeat payouts come from `combatDummyLifecycle`'s resurrect branch and that a follow-up hit past its gib threshold removes the dummy from the loop, and note in the dev-mod comments that both policies are reference content — the engine has no concept of a reward.

## Sequencing

**Phase 1 (sequential):** Task 1 — the chokepoint both entry points call.
**Phase 2 (concurrent):** Task 2, Task 3 — independent entry points over Task 1. Their Rust files are disjoint (`impact_policy.rs` / `impact_effects.rs` vs. the new `grant/` module and the trigger-binding files), but they share `sdk/lib/data_script.{ts,luau}`, the typedef templates, and the regenerated `postretro.d.{ts,luau}` plus drift fixtures — land the SDK edits in one pass or sequence Task 3 after Task 2.
**Phase 3 (sequential):** Task 4 — consumes both entry points.

## Invariants

| Invariant | Established by | Preserved / threatened at | Verified by |
|---|---|---|---|
| Grant only ever adds — never a damage path | Task 1 (negative/non-finite rejection before any write) | Both entry points must route through Task 1; a caller that writes a component directly bypasses it | AC 3, 4 |
| One health-write chokepoint owns clamp + resurrect re-arm | Task 1 (routes through the absolute-write function) | A health grant that clamps or stores inline would drop the re-arm silently | AC 5, 6 |
| A grant never fabricates state on the recipient | Task 1 (missing component → skip outcome) | Task 2 and Task 3 must log and continue, not abort the fire or the dispatch | AC 9 |
| Grant on the impact arm addresses the source only | Task 2 (token check at bind) | A future arm reusing the generalized checker must state its own expected token | AC 10 |
| A skipped grant never aborts sibling effects | Task 2 (per-effect skip), Task 3 (per-target skip) | Shared with the fire's evaluate-then-apply loop | AC 8, 9, 22 |
| Ammo enters a reserve *after spawn* through exactly one function | Task 1 | Task 3 and any future pickup/loot spec must call it rather than credit directly; descriptor spawn seeding in `seed_weapon_reserve` is the one deliberate second write path | AC 19 |

## Boundary inventory

| Name | Rust | Wire / serde | JS / TS | Luau |
|---|---|---|---|---|
| health grant (impact arm) | grant chokepoint fn | `{ primitive: "grantHealth", target: "@impact.source", args: { amount: IR node or number literal } }` | `impact.source.grantHealth(amount)` | `impact.source:grantHealth(amount)` |
| ammo grant (impact arm) | grant chokepoint fn | `{ primitive: "grantAmmo", target: "@impact.source", args: { type: string, amount: IR node or number literal } }` | `impact.source.grantAmmo(type, amount)` | `impact.source:grantAmmo(type, amount)` |
| health grant (reaction, tag form) | reaction primitive | `{ primitive: "grantHealth", tag: string, args: { amount: number } }` | `grantHealth(tag, amount)` | same |
| health grant (reaction, activators form) | reaction primitive | `{ primitive: "grantHealth", target: "@activators", args: { amount: number } }` | `grantHealth(on.activators, amount)` | same |
| ammo grant (reaction, tag form) | reaction primitive | `{ primitive: "grantAmmo", tag: string, args: { type: string, amount: number } }` | `grantAmmo(tag, type, amount)` | same |
| ammo grant (reaction, activators form) | reaction primitive | `{ primitive: "grantAmmo", target: "@activators", args: { type: string, amount: number } }` | `grantAmmo(on.activators, type, amount)` | same |
| source handle | command-target token `@impact.source` | existing token channel | `SourceHandle` (gains methods) | (table) |
| ammo pool key | reserve map key | `[A-Za-z0-9_.:-]`, max 64 bytes — `validate_ascii_identifier` at load, same gate as the descriptor's `resource.type`; no registry-membership check | `type: string` | same |

## Script syntax examples

```ts
// Proposed design — the engine has no concept of a reward; this is reference content.
// Filtered on the dummies, not `enemy`: combat-demo's one enemy-tagged entity
// is the zombie that resurrects, so it would never settle a kill edge.
const ammoOnKill = defineImpactEvent("dev:ammo-on-kill", { tag: "dummy" }, (impact) => {
  const killed = impact.target.healthBefore.gt(0).and(impact.target.healthAfter.le(0));
  return [
    // The kill EDGE, not a level gate: a level gate re-pays on every corpse hit.
    { when: killed, do: [impact.source.grantAmmo("bullets.light", 8)] },
  ];
});

// A glory-kill-style heal reads the impact's own facts to size the reward.
const healOnOverkill = defineImpactEvent("dev:overkill-heal", { tag: "enemy" }, (impact) => {
  const overkill = impact.target.healthAfter.times(-1);
  return [{ when: impact.target.healthAfter.le(-40), do: [impact.source.grantHealth(overkill)] }];
});

// The pickup: a trigger volume grants to whoever walked in. Not producer-gated.
// A primitive builder already returns a PrimitiveReactionDescriptor — no sequence wrapper.
// No explicit name: the only referrer is the onTriggerEvent below, which holds
// the const directly. An explicit dispatch address is load-bearing only for a
// map-brush KVP or cross-runtime TS/Luau agreement.
const ammoPickup = defineReaction((on: TriggerEventParams) =>
  grantAmmo(on.activators, "bullets.light", 24),
);
// returned from setupLevel():
//   { reactions: [ammoPickup],
//     triggerEvents: [onTriggerEvent({ tag: "ammo_pickup" }, "enter", [ammoPickup])] }
```
