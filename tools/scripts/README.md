# tools/scripts/

Placeholder for project automation scripts. Planned additions:

- `new-mod.sh` — scaffold a new mod directory with the expected layout
- `new-level.sh` — scaffold a new `.map` source file and asset folders

Nothing here yet. Add scripts as needs arise; keep them small, POSIX-friendly, and
self-documenting (`--help`).
