# Research — source anchors

Grounding for the graphics-mode-toggle spec. File:line drift expected; re-confirm before editing.

## Renderer

| What | Where | Note |
|---|---|---|
| Current world sampler | `crates/postretro/src/render/mod.rs` `create_mip_sampler` (~272) | Nearest mag/min, Linear mip, no anisotropy. Keyed per mip count, LOD-clamped to `mip_count-1`. |
| Sampler pool | `Renderer::mip_count_samplers: HashMap<u32, wgpu::Sampler>` | One sampler per distinct uploaded mip count. |
| Group-1 BGL | `render/mod.rs` (~866–919) | 0=diffuse, 1=`base_sampler`, 2=specular, 3=shininess uniform, 4=normal. Binding 5 free. |
| Forward shader source | `render/mod.rs` `SHADER_SOURCE = concat!(forward.wgsl, curve_eval.wgsl)` (~138) | Single pipeline `Renderer::pipeline` (~404). |
| Fragment entry / sample sites | `crates/postretro/src/shaders/forward.wgsl` `fs_main` (~404); diffuse ~406, normal ~418, specular ~497 | All `textureSample(_, base_sampler, in.uv)`. `base_sampler` at `@group(1) @binding(1)` (~38). No const/override toggles. |

## Runtime shader-knob precedent (copy this pattern)

| What | Where |
|---|---|
| Enum | `LightingIsolation` (10 variants) `render/mod.rs` (~172), `ALL_VARIANTS` (~188), `label()` |
| To shader | `FrameUniforms.lighting_isolation: u32` (~234), serialized ~258 |
| Setter | `Renderer::set_lighting_isolation` (~2120) |
| egui control | `render/debug_ui/mod.rs` `draw_diagnostics_panel` ComboBox (~150–161); `DiagnosticsState` mirrors values (~39) |

## Mod manifest

| What | Where | Note |
|---|---|---|
| Struct | `ModManifestResult { name: String, entities: Vec<EntityTypeDescriptor> }` `scripting/runtime.rs:27` | No serde; manual extraction. Stored in `ScriptRuntime.mod_manifest: Option<_>` (~77). |
| Parse/validate | `run_mod_init_quickjs` (~758), `run_mod_init_luau` (~905) | Missing `name` → `ScriptError::InvalidArgument`. `entities` optional → empty vec. |
| Apply site | `main.rs` boot `BootState::Splash` (~1258); start-script hot-reload (~648–665) | Drains `entities` into `DataRegistry` after init. |
| SDK type | `sdk/types/postretro.d.ts:172`, `.d.luau:171` | Generated by `gen-script-types`. Registered in `scripting/primitives/mod.rs:234` via `register_type("ModManifest").field(...)`; `?` suffix = optional. Parity guard test ~306–355 anchors registration to the struct. |
| Boot order | `Renderer::new` `main.rs:408` **before** `run_mod_init` `main.rs:1258` | Renderer cannot get the default at construction; apply via setter post-init. |
| Enum serde precedent | `scripting/components/light.rs:15` `LightKind`, `FalloffKind` | Plain `Serialize/Deserialize`; add `#[serde(rename_all=...)]` for renamed strings (manifest path is manual extraction, not serde). |
