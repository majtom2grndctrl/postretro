- `context/plans/drafts/script-driven-light-bake/index.md` assumes `sdk/lib/prelude.js` exists as a loadable file on disk for the compiler — see Scope bullet 2 (line ~18), Task 1 prelude-evaluation paragraph (line ~83), the "Prelude path in the compiler" sketch (lines ~138–142, walks upward to find `sdk/lib/prelude.js`), and the "Prelude availability" open question (lines ~167–169). After this plan, the prelude lives in `$OUT_DIR` and is embedded in the engine binary; `prl-build` will need its own copy (e.g. its own `build.rs` invoking `postretro-script-compiler::write_prelude`, or `include_str!` from `OUT_DIR`) rather than walking the source tree.
