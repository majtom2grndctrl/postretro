[package]
name = "postretro-combat-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Combat domain data shared by Postretro crates"
license.workspace = true

[features]
# Makes the pre-canonicalization payload constructor available only to dependent
# test targets that must prove the engine-side JSON codec clears local view feel.
test-support = []

[dependencies]
glam.workspace = true
serde.workspace = true
postretro-net.workspace = true
postretro-foundation.workspace = true
postretro-entities.workspace = true
