# Seat, Session Identity, and Roster

## Goal

Give the engine a durable per-player key. Today every per-player address dies at a boundary: entity ids die at level unload, connection ids die at reconnect, and no identity is assertable by the player at all. This spec mints three separate identities — session, seat, player — and uses the seat to carry health, ammo, and loadout across a level change and back to a player who drops and rejoins.

## Scope

### In scope

- Host-minted session id, one per hosted run.
- Host-minted seat, one per admitted peer plus one for the local player, surviving level transitions.
- Client-asserted device-local player id, generated on first run, persisted with player options, and carried in the connect token.
- Engine-owned roster keyed by seat, published to clients.
- Seat-hold window and reclaim-by-player-id on rejoin.
- Carried per-player state: current health, ammo reserve across all ammo types, per-slot magazines, inventory composition, active slot, spawn placement.
- One shipped defect fix: placement assignments survive a close but are keyed by a client id the reconnecting peer no longer has, and the level unload drops them along with the round-robin cursor.
- Behavior-preserving splits of the four production files this spec extends.

### Out of scope

- **Per-seat scalar state slots and the `perOwner` mod-store authoring surface.** `drafts/E16--per-player-currency` owns them. This spec mints the key that surface will use; it does not build the surface. Nothing here needs it: the rejection in `replication_scope_for` (`crates/scripting-core/src/store_bridge.rs`) applies to mod-declared stores only, and this spec declares none — every engine slot it touches already has owner-private replication. The carried set stays a structured record because two of its six values, the ammo-reserve map and the inventory composition, have no slot representation at all — see Direction.
- **Lobby UI, join predicate, session-phase vocabulary.** The lobby spec of the Epic 15 band, not yet drafted. `plans/done/E15--session-lifecycle/index.md`'s "Authored join policy" bullet forecloses the join predicate; `plans/roadmap.md`'s "Lobby authoring surface" sub-bullet under Phase 3.75 owns the lobby UI and the session-phase vocabulary.
- **Authentication.** `context/lib/index.md:102` names anti-cheat a non-goal of this project's multiplayer; `research/coop-session-lobby.md:80-84` states the friends-group trust posture and says plainly that the player id "is not an authentication mechanism." The connect token is unsecured and its claim is forgeable. Two forward-compat constraints are honored instead — invariants I3 and I4.
- **Host migration and session-state serialization.** `plans/done/E15--session-lifecycle/index.md:78-80` opened the ledger without serializing it. This spec adds two entries to that ledger and keeps the roster rebuildable (I5); it writes no serializer.
- **A split of `crates/postretro/src/main.rs`.** Owner decision. This spec edits it in place rather than splitting it; a 9,967-line split is its own project.
- **Client-side health as a component.** A connected client's pawn has never carried `HealthComponent`; health reaches it only as a replicated slot. Carry stays host-side.
- **Two shipped ammo-display defects, both predating this spec and neither caused by the seat.** Switching to a weapon with no ammo resource leaves the previous weapon's ammo on the HUD, because `AmmoSlotProjection` stops ingesting rather than clearing and only the local host publisher has a `clear_hud_slot` path. And a connected client's local reload machine can `take` from its own stale `AmmoReserve` while a switch is in flight; the value is never displayed and the next snapshot overwrites it. Both belong to whoever owns HUD ammo replication.
- **Authored carry policy.** Carrying is unconditional here. Pistol-start, per-level resets, and any authored opt-out are policy, not mechanism — see Direction. The seam is named and kept to one call per spawn path; no descriptor field and no knob ship in this spec.

## Boundary inventory

| Name | Rust | Wire / serde | JS / TS | Luau | FGD KVP |
|---|---|---|---|---|---|
| Session id | `SessionId([u8; 16])` — new, `crates/net/src/wire.rs` | `[u8; 16]` | n/a | n/a | n/a |
| Seat | `Seat(u16)` — new, `crates/foundation` | bare `u16`, no type | n/a | n/a | n/a |
| Player id | `PlayerClaimId([u8; 16])` — new, `crates/net/src/wire.rs` | `[u8; 16]` | n/a | n/a | n/a |
| Connect claim | `ConnectClaim` — new, `crates/net/src/wire.rs` | bitcode blob inside connect-token `user_data` | n/a | n/a | n/a |
| Roster entry | `RosterEntry` — new, `crates/net/src/wire.rs` | struct in `Vec<RosterEntry>`, reachable only under `RosterView::Full` | n/a | n/a | n/a |
| Roster view | `RosterView` — new, `crates/net/src/wire.rs` | enum, `Counts` or `Full` | n/a | n/a | n/a |
| Roster message | `ServerControlMessage::SessionRoster` — new, appended | positional tag, appended last | n/a | n/a | n/a |
| Roster message payload | `SessionRosterMessage` — new, `crates/net/src/wire.rs` | payload of the `SessionRoster` variant | n/a | n/a | n/a |
| Seat record | `SeatSessionState` — new, `crates/postretro/src/netcode/seat.rs` | **never crosses the wire** | n/a | n/a | n/a |
| Unbind edge | `ServerPoll.disconnects: Vec<ClientId>` — new field, `crates/net/src/transport.rs` | net-to-binary only, not a message | n/a | n/a | n/a |
| Replication scope (engine) | `postretro_entities::slot_table::ReplicationScope` (`slot_table.rs:52`, has `None`) | — | — | — | — |
| Replication scope (wire) | `postretro_net::state_slots::ReplicationScope` (`state_slots.rs:76`, no `None`) | — | — | — | — |

Two distinct types share the name `ReplicationScope`. They are not interchangeable and `entities` does not import net's; the mapping lives in the binary. Any task naming the type must qualify which one.

**`Seat` is the one identity that does not live in the net crate, and it must not acquire a second definition there.** `net` depends only on renet, renet_netcode, bitcode, and log — it is postretro-free by contract (`development_guide.md:28`) and cannot take a `foundation` dependency. `entities` may depend only on `foundation` (enforced by `layering_invariants_hold`, `crates/xtask/src/crate_graph.rs:496`). So no crate exists that both `net` and `entities` can name a shared type from. `foundation` is the only home from which the binary, `entities`, and any later floor-crate consumer can all name one `Seat`; the wire carries a bare `u16` and the net crate defines no seat type at all. Minting a `Seat` in `net` would force a duplicate in `entities` the first time per-seat storage reaches the floor slot table — the same split this table already documents for `ReplicationScope`, and the one I2 forbids.

`SessionId` and `PlayerClaimId` stay in `net`: they cross the wire and nothing below the binary names them.

Display names are UTF-8 `String` on the wire and are never used as a key.

## Wire format

Two new wire surfaces plus one net-to-binary field. One surface appends a variant to a shipped enum; whether that changes the enum's encoded layout is measured in Task 6, not assumed.

### Connect claim — connect-token `user_data`

Fixed 256 bytes (`NETCODE_USER_DATA_BYTES`), carried in `ClientAuthentication::Unsecure`, populated at `crates/net/src/transport.rs:764` where the client currently passes `None`. The host reads it with `transport.user_data(client_id)` on the `ClientConnected` edge in `collect_server_events` (`transport.rs:270-281`).

| Offset | Width | Content |
|---|---|---|
| 0..4 | 4 | Magic `b"PRSC"` |
| 4 | 1 | Format version, `u8`, currently `1` |
| 5..7 | 2 | Payload length, `u16` little-endian |
| 7..7+len | len | bitcode-encoded `ConnectClaim` |
| 7+len..256 | rest | Zero fill, ignored on read |

`ConnectClaim` derives native `bitcode::Encode`/`Decode` like every other wire type (`wire.rs:4`), holds `player_id: [u8; 16]` and `display_name: String`, and gains fields only by appending — bitcode tags positionally (`wire.rs:1048-1049`). `display_name` has no length bound of its own and the envelope is fixed at 256 bytes with a 7-byte header, leaving 249 bytes for the bitcode payload; pin `DISPLAY_NAME_MAX_BYTES = 200`, leaving 249 bytes less the 16-byte id and bitcode's length prefixes with room to spare. The encoder truncates on a UTF-8 boundary at that bound; the decoder rejects an over-long name as no claim. Both sides read the same constant — a mismatch produces a permanently anonymous seat with no error.

The magic prefix is load-bearing and not decoration. When a client passes `user_data: None`, renetcode fills the 256 bytes with **random** data rather than zeros, so an absent claim is indistinguishable from a present one without a marker. Magic mismatch, version mismatch, a length exceeding the remaining bytes, or a bitcode decode failure all mean *no claim*, and the host mints an anonymous seat that can never be reclaimed. This is the degradation path for a dev launch with no configured identity.

Endianness applies to exactly one field, the length prefix. No other multi-byte integer appears in the outer layout — the player id is opaque bytes and the payload is bitcode's own encoding — so there is one endianness decision, not a family of them.

This layout mirrors no existing section, because no existing section is a fixed-width transport field. The framing choice follows `ParityDeclaration` (`wire.rs:1110`) in using a bitcode struct for the payload, and adds the magic/version/length envelope only because the 256-byte field is fixed-width and pre-filled with noise.

### Session roster — `ServerControlMessage::SessionRoster`

A new variant **appended after `SwitchAccepted`** (`wire.rs:1260`), carrying `SessionRosterMessage { session_id: SessionId, your_seat: Option<u16>, view: RosterView }`. `RosterView` is `Counts { participants: u16 }` or `Full { entries: Vec<RosterEntry> }`; the redaction tier is explicit in the type, never inferred from an empty list. `RosterEntry` holds `seat: u16`, `player_id: Option<PlayerClaimId>`, `display_name: String`, `connected: bool`. Seats travel as bare `u16` because the net crate cannot name the `Seat` type — see the Boundary inventory.

Admission is the privacy boundary. An admitted peer has passed a host decision and receives `Full`; everything below admitted receives `Counts` — how many players are here, and nothing about who they are (I9). Display names and player ids exist only inside `RosterEntry`, so the redaction is structural rather than a field-level filter a later edit can forget.

Conventions followed from the crate: variable-length lists are a `Vec` of a named struct, never parallel arrays (`ShotVerdictsMessage`, `wire.rs:1087`); an empty `entries` under `Full` is valid and means an empty roster; `Option` carries an explicit documented meaning — `your_seat: None` means the recipient holds no seat yet, which is every pre-admission recipient, and `player_id: None` means an anonymous seat. Fixed-size identity is a bare byte array, not a `Vec` (`ParityDeclaration.mod_digest`, `wire.rs:1111`).

### Delivery latency

`NetServer::update` calls `send_packets` once, at its end. Anything the binary queues after the poll returns — the roster publish, the snapshot batch — leaves on the next frame's `update`. Every "within one snapshot" claim in this spec means one snapshot interval after that flush, not from the tick that produced the value.

### What does not cross the wire

`SeatSessionState` — the seat's carried record — stays host-local. The net crate carries seat *ids* and never seat *contents*, which is what keeps it registry-blind (I8).

Ammo does not cross here either. Loaded magazine and reserve for the equipped weapon already replicate as owner-private state slots at snapshot rate, and the carry rides that existing path — see Task 8.

## Invariants

| Invariant | Established by | Preserved / threatened at | Verified by |
|---|---|---|---|
| I1 — A seat sits above the participation lifecycle and is released only by hold expiry or by being superseded on reclaim | Task 2 (seat table), Task 7 (release path) | `networking.md`'s "any exit from participating clears that slot's state" rule fires on a level change, which is now such an exit; a mint or release rule keyed to participation churns every seat at every level change. Seat 0 is minted with the table and the rest at admission — a level change demotes to `Admitted` and never below, so both edges sit outside that sweep. A disconnect starts a hold rather than a release | AC-SEAT-2, AC-CARRY-1 |
| I2 — The seat is the sole key joining the roster to carried per-player state | Task 2, Task 6 (roster) | Any second per-player key added beside it re-creates the duplicate-store problem this spec exists to end | AC-ROSTER-1, AC-REJOIN-1 |
| I3 — The player id is opaque | Task 1 (claim decode), Task 7 (reclaim) | The engine must never parse, slice, derive from, or order by its bytes; equality at the reclaim chokepoint is the only permitted operation | AC-ID-3 |
| I4 — Seat reclaim is decided at exactly one chokepoint, the admission edge | Task 7 | Any second site that grants a seat from a claim makes a future auth change a multi-site edit. Admission is also where the host already decides whether a peer is a player, so the claim is evaluated once and only there | AC-ID-3, AC-ID-1 |
| I5 — Every identified roster entry is re-mintable under a new host from its player id alone | Task 6 | Session id and seats are host-minted and cannot survive a host change (`plans/roadmap.md:202`), so they are re-derived rather than preserved. No entry may carry a field that is host-minted *and* unrecoverable. An anonymous entry carries no id and cannot be rebuilt — the stated cost of allowing one, and why Task 1 generates an id rather than relying on the anonymous path | AC-ROSTER-2 |
| I6 — The within-level player address is unchanged | Task 2 | E17's `PlayerId` (`trigger_system.rs:22`) has three named consumers — trigger occupancy, the Use seam, and per-player `use_pressed`; the seat is a parallel key and replaces none of them | AC-E17-1 |
| I7 — Carried state is an enumerable set, not a scattered accretion | Task 2, Task 5 | `plans/roadmap.md:185` requires the session-state set be enumerable because host migration is that set plus a live-world layer; a value that carries without appearing in the ledger violates it | AC-LEDGER-1 |
| I8 — The net crate stays registry-blind | Task 1, Task 6 | `development_guide.md:28` plus `crates/net/Cargo.toml`'s dependency list warrant net's postretro-freedom; `layering_invariants_hold` asserts nothing about `net` until Task 6 extends it. Seat ids may cross the wire, seat contents may not | AC-BUILD-1 |
| I9 — A display name never reaches an unadmitted peer | Task 6 (roster view) | Admission is the only host decision a peer has passed; handles published below it leak player identity to anyone who can open a socket. `RosterView::Counts` is the only shape an unadmitted slot may receive, and names live only inside `RosterEntry` | AC-ROSTER-3 |

## Ordering matrix

| Scenario | Ordering | Expected outcome |
|---|---|---|
| Harvest vs. entity despawn | Two hooks, two constraints: teardown entities stay live until `clear_for_level_unload`, and the lifecycle arm's pawn plus its sibling wieldables die inside `on_slot_closed` | The teardown harvest runs ahead of `clear_for_level_unload`; the lifecycle harvest runs as the first statement of the `Closed \| Demoted` arm, ahead of `on_slot_closed`. Both are independent of `data_registry.clear()`, because canonical names come off `DescriptorProvenance` |
| Demote without an unload | A mod reload drives `set_mod_digest` into `reevaluate_parity`, demoting a live participant; `host_handle_lifecycle` despawns the pawn on the `Closed \| Demoted` arm with no level change in sight | Harvest runs against the live pawn before that despawn. Without it the seat keeps values from the previous unload and seeds them over a live session |
| Platform suspend with participants | `App::suspended` calls `clear_net_level_parity` directly, bypassing `unload_level` | Harvest runs. A hook placed inside `unload_level` alone never fires here |
| Demote and disconnect on the same tick | Both drain from the same poll | Disconnect wins. The seat enters its hold window; the demote is absorbed |
| Drop while demoted, mid-level-load | A level change demotes to `Admitted`, then the peer times out. `SlotTable::transition` emits `Closed` only from `Participating`, so no `SlotEvent::Closed` is produced | The hold still starts, because hold-start keys to the transport's `ClientDisconnected` edge rather than the slot event. Roster flips to `connected: false` |
| Level change while a seat is held | Hold is session-scoped; the level boundary does not touch it | Held seat keeps its last harvested state and seeds nothing, because it owns no pawn |
| Level change with no pawn for a slot | A level may resolve no `player_spawn`, leaving a fly-camera and no pawn (`entity_model.md:198`) | Harvest finds no pawn and leaves the seat's prior state intact rather than overwriting it with empty |
| Hold expires during a level install | Expiry is evaluated at one per-tick chokepoint | Release never lands mid-install; it applies at the next chokepoint after the install completes |
| Reclaim after hold expiry | Seat already released and removed from the roster | Fresh seat at descriptor defaults. No error; the player simply starts over |
| Two live connections assert the same player id | Second claim arrives while the first holder is still connected | Second gets a fresh seat, no reclaim, and the host logs a warning. The first holder is never displaced |
| Rejoin before the host processed the close | renet mints a new client id; the prior slot is still participating or admitted | Treated as the two-live-connections row above until the prior slot times out |
| Claim absent or undecodable | Magic mismatch, version mismatch, over-long length, or bitcode failure — including the random bytes renetcode writes for `user_data: None` | Anonymous seat, no reclaim, warning logged once per connection |
| Reclaim vs. a never-connected tombstone | `transition` plants a permanent `Closed` tombstone for ids that never connected, to refuse stale packets (`transition`'s unknown-close branch) | Never interact. Reclaim rebinds a seat to a fresh client id and never revisits a closed entry, so `SlotTable` needs no discriminator and terminality stays intact |
| Session with zero remaining participants | Every seat held or released; roster may be empty | Session id persists for the process. An empty roster is a valid roster |
| Roster publish vs. admission | `drain_control` delivers payload-bearing frames regardless of participation epoch, so an admitted slot can be sent a roster | Full roster publishes on entry to admitted and on every change thereafter. A slot below admitted receives counts only, and crossing into admitted republishes as `Full` |
| Roster publish to a slot that never gets admitted | A peer connects, is refused admission, and is closed | It received only `Counts` for its whole life. No name or player id ever reached it |
| Disconnect while `Pending` | `ClientConnected` stashes the claim, `ClientDisconnected` arrives before admission | No seat was minted, so no hold and no roster entry; the claim stash entry is removed |
| Disconnect and rejoin in one event drain | `collect_server_events` yields `ClientConnected(B)` before `ClientDisconnected(A)` for the same player | `NetServer::update` runs `process_control_messages` strictly after `collect_server_events`, so a same-poll unbind is always visible to a same-poll admission. Reclaim never sees a seat still bound. Pin that call order as contract, not as an accident of the current body |
| Two held seats match one claim | A player holds a stale seat from an earlier drop and a recent one; one connect matches both | Most-recently-held wins; the loser is released immediately rather than left held |
| Entry and demote in one poll | One poll yields `Participating{c}` then `Demoted{c}`; `is_current_participation_entry` skips materialization | The seat was already minted at admission and is untouched by either event: seat count 1, pawn count 0 |
| Admitted but never participating | A peer is admitted, held for content parity, and never promoted | It holds a seat, appears in the roster as `connected: true`, and receives `RosterView::Full`. It carries nothing, because it never owned a pawn |
| N seats change in one poll | Three closes drain from one `ServerPoll.lifecycle` | One publish per poll carrying the final roster, not three carrying intermediates. No intermediate roster is observable; N=0 publishes nothing |
| Roster publish to K recipients | One roster change fans out | `your_seat` is recomputed per recipient; a single encoded message reused across recipients ships one client's seat to all of them |
| Roster publish vs. the despawn it describes | The unbind edge marks the seat disconnected inside `NetServer::update`; harvest and despawn run afterward, in the lifecycle drain | The mark precedes the despawn and cannot be reordered. The guarantee comes from deferring the publish instead: one publish per frame, after the lifecycle drain, so no observer sees `connected: false` beside a live pawn |
| Placement cursor after a level change | Level 1 assigns placements 0 and 1, leaving `next_assignment` at 2; the struct replacement resets it to 0; a new client joins level 2 | The cursor moves onto the seat table, and assignment scans past indices a live seat already holds. Carrying the cursor alone is not enough: `next_assignment % placement_count` is 0 again in a two-placement level, the same collision |
| Carried placement into a smaller level | Seat holds placement index 6; the next level resolves two placements and `assign_placement` clamps to 1, which another seat owns | Out-of-range carried placements are re-assigned from the cursor rather than clamped into a collision. AC-REJOIN-1's "previous spawn placement" is scoped to a rejoin within one level |
| Close-time harvest after the world is gone | Level change harvests at t=0, registry clears, the client times out at t=2 while Loading | The close-time harvest is a no-op that leaves the unload harvest's record intact, never an overwrite with empty |
| Seat allocator exhausted | The 65,536th mint attempt | The mint is refused, logged, and the slot closed. Admission-stage closes carry a typed cause and wait on its delivery, so this reuses the existing refusal cause rather than adding a variant; the player sees a refusal, not an unreachable host. No number is ever returned twice |
| Claim stash outliving its connection | A client connects, never participates, disconnects | The stash entry is removed at the disconnect edge; repeated cycles leave stash length 0 |
| Relay-added connection with no netcode entry | `add_relay_connection` seeds a slot via `slots.on_connect` with no netcode entry, so `transport.user_data` has nothing | An anonymous seat is minted rather than the mint failing. `add_relay_connection` is called from `trigger_state_channel_harness_test.rs` and two tests in `netcode/mod.rs`, all binary-side where the anonymous mint applies, and from `harness.rs` and `transport.rs`'s own tests, which never reach the seat table |
| Seat seed vs. tuning send | The promotion seam runs spawn, then `resolve_accepted_host_pawn_presentation`, then `tuning_payload_for_pawn`, then `host_send_tuning_if_changed` | The seat seed lands inside the spawn call, before `tuning_payload_for_pawn` reads the live inventory. A seed applied after that read ships descriptor defaults to the client |
| Hold window at 0 or below one tick | Unbind edge starts the hold; the expiry site evaluates on that tick or the next | Seat released at the first evaluation, one roster publish with the entry gone. No reclaim is ever possible, so the manual gate rejects these values |
| Hold across a long level install | `unload_level` sets `BootState::Frontend`; the host sits there for minutes | The hold clock is accumulated sim delta and does not advance. The seat survives an install of any length, and a suspend of any length |
| Level change with N participants | `clear_net_level_parity` demotes all N; the `Demoted` events drain on the next `NetServer::update`, after the teardown harvest and after the registry cleared | For every N including 0 the lifecycle arm finds no live pawn and no-ops. The teardown record stands, no seat is zeroed, seat count unchanged |
| Unbind and admission in one poll | `collect_server_events` records A's disconnect at the top of `NetServer::update`; `process_control_messages` admits B asserting A's player id later in the same call | B reclaims A's seat in that poll. A's seat never appears in any published roster as held, because both mutations precede the single per-poll publish |
| Claim stash vs. same-poll admission | `ClientConnected` stashes the claim in `collect_server_events`; the admission control message is processed later in the same `update` | The stash is populated before admission reads it. This ordering is the only reason a same-poll connect-and-admit is not anonymous |
| N player-start entities | `spawn_from_player_starts` spawns one pawn per entry and marks the first `PlayerMovement` one local | Seat 0 seeds that pawn only, for every N. At N=0 no seed runs and seat 0's record is left intact |
| Roster mutates at several sites in one frame | A unbinds, B is admitted, A's pawn despawns, C's hold expires | Exactly one publish, after the last mutation, carrying A disconnected, B present, C absent. No publish is emitted from any mutation site |
| No roster change in a poll | A poll drains with no connect, disconnect, admission, or expiry | Nothing is sent. The per-frame publish is a dirty-flag drain, not an unconditional send |
| Harvest after a seed, before any snapshot | Seat seeded at level install, peer times out before the first snapshot | The close-time record is byte-identical to the seeded one. Harvest-after-seed is a lossless round trip through canonical names, magazines, active slot, and reserve |
| Harvest from a pawn with no inventory or health | A `player_spawn` descriptor authors neither block | The seat's prior record is preserved, not overwritten with empty. Harvest is non-destructive in the same way the seed is |
| Seat seed vs. `refresh_host_tuning` | A mod reload promotes a held slot inside `set_mod_digest`; `refresh_host_tuning` runs immediately over `participating_clients()`; the pawn spawns on the next poll | `refresh_host_tuning` finds no bound pawn and sends nothing. The payload is built once, at the promotion seam on the next frame, after the seed |
| Repeated admission of one client id | `transition` returns `None` for `Pending -> Admitted` and `Admitted -> Admitted`, so `admit()` emits no event | Exactly one seat per client id per connection for every N attempts including N>=2. The mint keys to `HandshakeOutcome::Admitted`, which is emitted once |

## Direction

**Problem.** The engine has no key that names a player for longer than one level or one connection, so every feature needing per-player state either dies at a boundary or is parked.

**Prior commitments.** The session-state ledger (`networking.md:149-153`) holds one entry, the connection, and says later specs add the seat and the roster to it — this spec is that addition. `plans/done/E15--session-lifecycle/index.md` carries an explicit out-of-scope bullet beside that same ledger citation — "Reconnect after a close. A closed connection stays closed. Demotion keeps a connection alive; a dropped peer relaunches" — which Task 7 reverses. The reversal is deliberate: the relaunching peer now rebinds a held seat rather than starting over. The admission/parity split turns on mutability (`networking.md:75-87`): the player id is *carried, never compared*, so it gates nothing and cannot convert a recoverable difference into a disconnect. `plans/roadmap.md:202` requires the roster be rebuildable from player ids alone; I5 honors it as far as it goes, re-minting every identified entry under a new host and naming the anonymous entry as the one case that cannot be. E17 said not to invent a heavyweight identity system (`plans/done/E17--trigger-command-surface/index.md:361-368`); I6 keeps its address intact and adds a parallel key.

*`networking.md:102` reads stronger than it is.* It says a client id is stable across a level change, "which is what later specs key player identity to." Client ids are wall-clock nanos minted per connection (`NetEndpoint::from_role`, `NetRole::Connect` arm, `netcode/mod.rs`), so that stability holds within one connection and not across a rejoin. This spec keys the *seat* to admission and the *reclaim* to the player id. The doc line needs narrowing at promotion.

*Why admission, not participation or connection.* Three edges were available. Participation is wrong because a level change drives every participant out of it, which is the churn I1 exists to escape. Connection is wrong because a seat number is never reused (AC-SEAT-1), so minting at connect lets anyone who can open a socket allocate seats without bound. Admission is the host's own decision point, it sits above the level-change demote floor, and it is where the roster's privacy boundary already falls (I9). One edge answers all three.

*Placement in the layer stack.* The seat **table** lives in the binary's `netcode/` module: it holds engine types, and `entities` is a compile chokepoint with six dependents including the whole render and UI stack that `development_guide.md:41` keeps domain logic out of. The nearest existing thing to a seat — `placement_assignments` (`netcode/lifecycle.rs:35-42`), a per-client map that survives a close but not a level unload — is already there. Session id and player id live in `net` alongside the other wire identities, mirroring `NetworkId` — a wire type whose allocator lives in the binary. For the seat **id type**'s placement, see the Boundary inventory.

*Carry is mechanism; whether a level carries is policy.* This spec makes the seat able to carry and makes carrying unconditional, matching the continuous-progression shape the campaign wants today. Pistol-start is a live design axis for a Doom-lineage shooter, and the repo's mechanism-versus-policy thesis is explicit — `plans/done/E16--impact-policy-substrate` builds a substrate holding no opinion about what a hit means. The opt-out seam is named rather than built: seeding reads the seat's carried record through one call per spawn path, so a policy that suppresses carry for a level suppresses that read. No authored knob ships here; adding one is a descriptor field and a check at that call, not a redesign.

**Alternatives rejected.**

*Key everything by the client-asserted player id and mint no seat.* One identity instead of three, trivial rejoin, a trivially rebuildable roster. Rejected because the player id is client-chosen and forgeable: making it the live key means a forged claim steals *live* session state rather than merely claiming a released one, and it makes admission stop being a host decision. The seat gives the host a value it controls. It also makes the id load-bearing everywhere, which is precisely what a later auth epic would have to unwind.

*Carry state through the existing `persist` save path.* Rejected on two shipped rules: a connected client does not write the save file (`should_save_persisted_state`), and client-to-host state writes are a stated Phase 3.5 non-goal. Persist is also machine-scoped where the carry is session-scoped — the wrong lifetime.

*Carry through per-seat state slots instead of a structured record.* The strongest rival, and the one that would also serve the currency spec. The state store has engine-global lifetime and is never cleared on level unload (`scripting.md:110`), so this path would delete the harvest hook and every harvest row in the Ordering matrix outright.

Rejected on shape. `SlotType::Array` does exist, so magazines fit, and health and active slot are `Number`. But `SlotValue::Array` is `Vec<f32>` — floats only. Inventory composition is a list of canonical *name strings* and has no representation at all, and the ammo reserve is a map keyed by ammo-type string, which no slot type expresses. Composition is the load-bearing value: without it the seat cannot rebuild a loadout, which is the carry's whole point. So a structured record is required regardless of what else moves into slots, and taking this path would also mean absorbing the `perOwner` authoring axis that `drafts/E16--per-player-currency` owns.

The seat still stands as the key a scalar per-seat axis will use when that spec builds one — same key, two shapes, one store each with no overlap in what they hold.

**Forecloses.** Fixing the claim in the connect token means it cannot change mid-connection — intended, and what makes it admission-stage-legal. The 256-byte budget forecloses a large profile payload; the envelope and a 16-byte id leave roughly 230 bytes, of which `DISPLAY_NAME_MAX_BYTES` claims 200. Monotonic non-reused seats foreclose a dense seat index; nothing wants one. Nothing here forecloses an authored carry policy: the seed path is one call per spawn path, and suppressing it is a check at that call.

**One-way doors.** None. The connect-claim layout is versioned and the version byte is checked, so a format change costs a protocol bump that admission already gates. Reclaim does not touch closed-slot terminality: a rejoining peer arrives on a fresh client id and never meets the closed entry, so the stale-packet guarantee stands and no shipped contract changes. `Seat`'s crate home is a door in practice rather than in principle — the type is trivial to move, but once consumers build against a net-crate seat, the duplicate-type shape is what gets extended; placing it in `foundation` now costs nothing. The carry inversion is fully reversible — deleting the harvest and seed hooks restores descriptor seeding.

## Acceptance criteria

- [ ] **AC-CARRY-1** — A player crossing a level boundary keeps their current health, their ammo reserve for every ammo type they hold, each weapon's loaded magazine, their weapon loadout, and the weapon they were holding.
- [ ] **AC-CARRY-2** — A player at full defaults, a player mid-magazine, and a player at zero reserve all carry correctly; a player who crossed the boundary at or below zero health seeds the descriptor default rather than a dead pawn; a level that spawns no pawn leaves prior carried state unchanged rather than clearing it.
- [ ] **AC-SEAT-1** — The local player holds seat 0, every admitted peer holds a distinct seat above it, a peer below admitted holds none, and a released seat's number is never reissued for the life of the session. A peer admitted after a level change receives a spawn placement no live seat already holds.
- [ ] **AC-SEAT-2** — A host level change does not release any seat, and every participant's seat is the same before and after.
- [ ] **AC-REJOIN-1** — A player who disconnects and rejoins within the hold window resumes with the health, ammo, and loadout they left with, and at their previous spawn placement when the rejoin lands in the same level; across a level change the placement is re-assigned from the cursor.
- [ ] **AC-REJOIN-2** — A player who rejoins after the hold window expires starts at descriptor defaults, and the session continues without error.
- [ ] **AC-ID-1** — A first-run client generates an identity, persists it, and asserts the same identity on its next launch. A client whose identity cannot be loaded or written still joins successfully, is reported as anonymous, and on rejoin receives a new seat rather than reclaiming the old one.
- [ ] **AC-ID-2** — A second client asserting an identity already held by a live participant joins with its own new seat, the existing participant is undisturbed, and the collision is logged.
- [ ] **AC-ID-3** — Reclaim succeeds only on an exact whole-identity match; a claim differing in any byte gets a fresh seat.
- [ ] **AC-ROSTER-1** — The roster lists every seat with its connection state, survives a level change unchanged, and drops a seat when its hold expires.
- [ ] **AC-ROSTER-2** — Every roster entry carrying a player id can be re-minted under a new host from that id alone; seats and connection state are re-derived rather than preserved. An anonymous entry cannot be, and the test states that as the expected result rather than asserting a rebuild.
- [ ] **AC-ROSTER-3** — An admitted peer receives the full roster. A peer that has not been admitted receives participant counts only: no display name and no player id reaches it, and no roster message it could receive has a shape that carries one.
- [ ] **AC-LEDGER-1** — The set of values that survive a level transition is enumerated in one place, and adding a value to the carry without adding it to that enumeration fails to compile.
- [ ] **AC-LEDGER-2** — The seat and the roster appear as entries in the session-state ledger `networking.md:149-153` opened, alongside the connection.
- [ ] **AC-E17-1** — Trigger occupancy, the Use interaction, and per-player use state behave exactly as before for both local and remote players across a level change.
- [ ] **AC-CLIENT-1** — Within one snapshot of a level change or a rejoin, a connected client's displayed ammo, reserve, and active slot match the host's authoritative values for the equipped weapon, and all three re-target on a switch between two weapons that both carry an ammo resource. Switching to a weapon with no ammo resource is the shipped HUD defect named out of scope and is excluded.
- [ ] **AC-BUILD-1** — `layering_invariants_hold` gains an assertion that `postretro-net`'s internal dependency set is empty, and it passes. Each split file's tests pass with identical counts before and after, with no call-site path changes.
- [ ] **AC-HYGIENE-1** — Repeated connect and disconnect cycles do not grow this spec's own bookkeeping — the claim stash and the seat table — without bound. `SlotTable`'s tombstone growth is pre-existing, untouched here, and out of scope.

## Tasks

### Task 1: Connect claim on the wire

Add the client-asserted identity to the connect token and read it host-side. Define `PlayerClaimId([u8; 16])`, `SessionId([u8; 16])`, and `ConnectClaim { player_id, display_name }` in `crates/net/src/wire.rs`, deriving native `bitcode::Encode`/`Decode` per that file's convention — no serde, and any future field is appended because bitcode tags positionally. Define `Seat(u16)` in `crates/foundation`, not in the net crate — see the Boundary inventory. Seats cross the wire as a bare `u16`; the net crate defines no seat type. Implement the 256-byte envelope: magic `b"PRSC"` at bytes 0..4, version `1` at byte 4, `u16` little-endian payload length at bytes 5..7, bitcode payload following, zero fill to 256. Provide encode and decode helpers where decode returns "no claim" for magic mismatch, version mismatch, a length exceeding the remaining bytes, or a bitcode failure — this matters because renetcode fills an absent `user_data` with **random** bytes, not zeros, so absence is only detectable by the marker. Thread an `Option<[u8; 256]>` parameter through `NetClient::new` (`transport.rs:750-756`, currently hardcoding `user_data: None` at `:764`) to all six call sites: `netcode/mod.rs:788`, `:3816`, `:3870`, `netcode/trigger_state_channel_harness_test.rs:298`, `net/src/harness.rs:280`, `transport.rs:1038`. `netcode/mod.rs:743` is not a call site; it is the doc contract on `NetEndpoint::from_role` and must stay consistent with the change. Host-side, read `transport.user_data(client_id)` inside the `ClientConnected` arm of `collect_server_events` (`transport.rs:270-281`) and stash the decoded claim on `NetServer` alongside the existing per-client side tables; the claim must be read on that edge and stashed, because renet_netcode drops the netcode entry when the connection tears down and it is not retrievable later. Add `disconnects: Vec<ClientId>` to `ServerPoll` in the same task, populated in `collect_server_events` on every `ClientDisconnected` regardless of what `close_slot` returns — Task 7's hold-start has no other observable, and the field is an id with no payload, so net stays registry-blind. Do not reuse the existing private `pending_disconnects`; that is the transport's own deferred-teardown queue, drained by `apply_pending_disconnects`, and it means something else. The stash entry is removed at that same edge alongside `close_slot`'s existing removals of `parity_declarations`, `holding_diagnostics`, and `participation_epochs`, and the seat copies the claim at mint time so this removal is safe. `NetcodeServerTransport::user_data(&self, ClientId) -> Option<[u8; 256]>` and renetcode's `generate_random_bytes` fill for an absent `user_data` are both confirmed against the vendored crate. The net crate holds the claim as opaque bytes and never interprets them (I3, I8). Expose a read accessor for the binary. No seat, no roster, no carry in this task.

Source the id client-side in this task too, or the claim has nothing to carry and every shipped client is anonymous forever. Add it to `PlayerOptions` (`crates/postretro/src/options/mod.rs`): 16 random bytes generated when the field is absent on load, written back through the existing atomic save, and read at startup to populate the claim. No new file and no new machine-scoped store — `settings_path` already resolves `<platform config dir>/settings.toml` and the save is already tmp-plus-rename. A missing or unwritable settings path is the anonymous degradation path, not an error: the client still connects, and the magic-marker envelope already handles the absent-claim case.

### Task 2: Seat table, session id, and the thin slice

Build the seat, mint it, and carry exactly one value end to end so the seams are falsified before anything fans out.

**Define the seat's three edges first.** The design needs three moments the shipped code does not expose under those names. Define them here; every later task keys to the edge, never to the function behind it.

| Edge | Observable it keys to | Why the obvious observable is wrong |
|---|---|---|
| Seat mint | `HandshakeOutcome::Admitted` in `ServerPoll.handshakes` | `SlotTable::transition` returns `None` for `Pending -> Admitted`, so admission emits no `SlotEvent` and there is nothing in `ServerPoll.lifecycle` to key to |
| Seat unbind | A new `disconnects: Vec<ClientId>` on `ServerPoll` | `collect_server_events` pushes to `pending_lifecycle` only when `close_slot` returns `Some`, which `transition` produces only from `Participating`. A drop while `Admitted` — the level-load case Task 7 exists to serve — reaches the binary through nothing at all today |
| Seat harvest | A binary-side call placed immediately before each player-pawn despawn | No shipped event marks a pawn as about to die; the two despawn sites are named below |

`disconnects` is net's only new surface. It carries an id and nothing else, so I8 holds. `collect_server_events` populates it on every `ClientDisconnected` regardless of what `close_slot` returns, and the binary drains it before `ServerPoll.lifecycle` in the same poll, so an unbind never lands after the harvest that describes it.

Create `crates/postretro/src/netcode/seat.rs` owning a `SeatTable`: a monotonic `Seat` allocator that never reuses a number, a map from seat to `SeatSessionState`, a map from seat to the currently bound client id, the round-robin placement cursor, and the session id minted once with the table. The table lives on `Session` beside `net_endpoint` and reaches its consumers as a parameter: the spawn paths and the composition chokepoint take a carried record, never a borrow of the table, so nothing below `netcode/` learns the seat exists. `SeatSessionState` lands with its **full field set** in this task even though only `health_current: Option<f32>` is populated here — Task 5 fills the rest with behavior, not with new fields, so no later task reshapes a struct another is editing. The seat also owns its own `pawn: Option<EntityId>` binding, set at spawn and cleared at despawn; the harvest resolves through it rather than through `SlotPawns`. The table lives in the binary because it holds engine types and because `entities` is a compile chokepoint that domain logic stays out of (`development_guide.md:39-41`). The `Seat` type itself comes from `foundation` (Task 1) and is not redefined here. Seat 0 is reserved for the local player and minted when the seat table is created, carrying the host's own persisted claim from `PlayerOptions` so it is a rebuildable roster entry like any other (I5). It appears in the roster and counts toward `participants`. The table is created with the session rather than with the network endpoint, so single-player and listen host both have one — otherwise `spawn_from_player_starts` seeds from a seat that cannot exist, and the host's carry needs a second mechanism, which is the duplicate-store problem I2 forbids. A connecting client mints no seat table; it holds only the `your_seat` the host sends it. Mint every other seat on the mint edge above, binding it to that client id, and record the claim Task 1 stashed at that client's connect edge. **Mint at admission, not at participation** (I1) — `networking.md`'s "any exit from participating clears that slot's state" rule fires on a level change, so a rule keyed to participation churns every seat at every level change, which is the exact bug the seat exists to prevent. A level change demotes to `Admitted` and never below, so the admission edge sits outside that sweep. It is also the edge Task 7 reclaims at and the edge the roster's privacy tier turns on (I9), so all three key to one place. Harvest keys to pawn despawn, not to level unload. Two hooks, two ordering constraints. The level-teardown harvest runs ahead of `clear_for_level_unload`, hooked in `unload_level` and in `App::suspended` **beside** their calls to `clear_net_level_parity` rather than inside it — that function early-returns when the session holds no `net_endpoint`, which is exactly the single-player case, so a harvest placed inside it never fires for a single-player level change and AC-CARRY-1 fails on the primary shipping path. The lifecycle harvest runs as the first statement of the `Closed | Demoted` arm of `host_handle_lifecycle`, ahead of `on_slot_closed`, which despawns the pawn and every inventory sibling wieldable before returning — and composition and magazines are read off those wieldables.

The lifecycle hook is a guard more often than a harvest. `clear_net_level_parity` demotes every *participating* slot — an already-`Admitted` slot produces no event, since `transition` returns `None` for `Admitted -> Admitted` — and those `Demoted` events do not drain until the next `NetServer::update`, by which point the teardown harvest has already run and the registry is cleared. So on every host level change the lifecycle arm meets a seat whose `pawn` no longer resolves: it must check the binding against the live registry and no-op otherwise, leaving the teardown record intact rather than overwriting it with empty. It does real work only on a mod-reload demote, where no teardown ran. The teardown harvest clears the seat's `pawn` binding; the lifecycle arm never leaves a stale one behind.

The harvest is deliberately independent of `reset_level_scoped_host_state` replacing `SlotPawns`: the seat resolves its own pawn, so the client→pawn map's fate is irrelevant. Canonical names come off the entity through `DescriptorProvenance`, not the data registry, so `data_registry.clear()` does not bound it. Seed the harvested health back onto the pawn after spawn on the two paths that attach `HealthComponent` — `spawn_from_player_starts` and `spawn_net_slot_pawn` — as a post-spawn override rather than by threading into `attach_descriptor_components`; on both paths the seed lands after their call to `spawn_descriptor_instance`, which is where health actually arrives. `spawn_from_player_starts` loops over every `player_spawn` entity and spawns one pawn per entry, marking only the first with `PlayerMovement` as local via `mark_local_player_pawn`, guarded on `local_player_pawn().is_none()`. Seat 0's record seeds that pawn alone; the other player-start pawns take descriptor defaults. Do not add health to the connected-client path: that pawn has never carried `HealthComponent` (`remote_materialize.rs:143-199` attaches movement, inventory, mesh, and viewer role only), and its health arrives as a replicated slot. Keep E17's `PlayerId` untouched (I6). `host_handle_lifecycle` has two production call sites — the running-frame host arm and `poll_world_less_transport`, the latter driven from `run_loading_frame` and from Frontend/Splash — and a disconnect during a level load is observed only at the second, so both are hook targets for the mint and harvest handling this task adds. Sequenced after the splits so the edits land in the post-split files.

### Task 3: Split `netcode/mod.rs`

Behavior-preserving split, no functional change. The file is 5,985 lines with production ending at 3,203. Extract only what later tasks extend, on the same standard Task 9 applies to `data_archetype.rs`: splitting the rest would move tests without reducing risk. Two regions come out. First, the `NetEndpoint` enum and its impl, including its `#[allow(clippy::large_enum_variant)]` attribute and the three reset functions `reset_level_scoped_client_state`, `reset_level_scoped_host_state`, and `reset_state_slot_schema` — this is the region Tasks 2, 6, and 7 edit most. Second, host tick and accept/register through `host_handle_lifecycle`, which Task 2 hooks. Everything else stays put.

Express both regions by first and last symbol, never by line: every line boundary in this file orphans an attribute or a doc comment from the item below it. Move the corresponding `#[cfg(test)] mod tests` blocks with the code they cover. Public and `pub(crate)` paths must keep resolving — re-export from the parent module rather than rewriting call sites across the binary. Run the full crate test suite before and after and confirm identical pass counts.

### Task 4: Split `startup/lifecycle.rs`

Behavior-preserving split, no functional change. The file is 4,407 lines with production ending around 1,934. Same standard as Tasks 3 and 9: extract only what later tasks extend. Two regions, expressed by symbol rather than by line, since the ranges here overlap and an `impl App` block spans them. First, the net-parity and unload cluster: a contiguous run of five, in source order — `install_network_mod_content`, `refresh_host_tuning`, `clear_surface_lifetime_level_state`, `unload_level`, `clear_net_level_parity`. Second, the CPU install segment including `install_world_cpu` and `install_descriptor_player_health_range`. Task 2 hooks the first and Task 5 lands in the second. Everything else stays put. Splitting an inherent `impl App` across modules is legal within the crate, so no region starts at the block header. Keep the ordering inside `unload_level` byte-for-byte identical — Task 2's teardown harvest lands ahead of `clear_for_level_unload`, so relative order within the unload cluster must not change. Move tests with their code, keep paths resolving via re-export, and confirm identical pass counts before and after.

### Task 5: Full carried set

Extend the carry from health alone to the whole per-player set: current health, ammo reserve across every ammo type held, each inventory slot's loaded magazine, the inventory composition, the active slot, and the spawn placement. Enumerate `placement` alongside the rest of the carried set; the field itself lands on the seat in Task 2. Store composition as canonical names, never entity ids — `clear_for_level_unload` despawns everything and bumps generations (`registry.rs:1036-1055`, `:938-943`), so no `EntityId` survives. Canonical names are readable from a live weapon through `DescriptorProvenance.canonical_name` (attached by `attach_descriptor_components`), and the restore path already exists: `compose_wieldable_inventory_from_slots` takes `&[Option<String>; WIELDABLE_SLOT_CAPACITY]` (`data_archetype.rs:112-129`). The carried record reaches the composition chokepoint as a parameter, never by reaching back into `netcode/`. `compose_wieldable_inventory_slots` lives in `scripting/builtins/` and the seat table lives in `netcode/`; the function stays seat-blind and takes an optional carried loadout, and each caller resolves the seat before calling. Resolution differs per path: `spawn_from_player_starts` uses seat 0, `spawn_net_slot_pawn` uses the seat bound to that client id, and the connected client's own path passes `None` because a client owns no seat table. Same shape that keeps `net` registry-blind, one layer up. Seed through the single composition chokepoint `compose_wieldable_inventory_slots` — all three spawn paths reach it, and it is the only writer of inventory *composition* (the `wieldables` array) and the only caller of `seed_weapon_reserve`. It is private, so no spawn path calls it directly: `spawn_from_player_starts` and `spawn_net_slot_pawn` reach it through `compose_wieldable_inventory`, and the client path through `compose_wieldable_inventory_from_slots`. The carried loadout threads through both wrappers. `compose_wieldable_inventory` has a no-loadout early return that writes a default `Inventory` and never reaches the chokepoint — a carried loadout must suppress that return, or the carry is silently dropped for any pawn descriptor with no authored loadout. `Inventory` as a whole has other writers: `apply_host_switch_declaration` (`netcode/mod.rs`) and the switch/lower path in `sim/weapon_stage/commands.rs` both write it, and `compose_wieldable_inventory` itself writes it on its own no-loadout early return. `active_slot` is separately written by `apply_host_switch_declaration` and the weapon-switch commands, and the carried-active-slot restore must not fight them. Two shipped gaps make this more than a hook. `active_slot` is currently *derived* — first populated slot wins, the `position(Option::is_some)` derivation in `compose_wieldable_inventory_slots`. Carry it as a parameter into the chokepoint, not as a post-composition override: an override has to be ordered against `apply_host_switch_declaration` and the weapon-switch commands, which is the fight this task must not pick. And `AmmoReserve`'s whole surface is `new`, `available`, `credit` (saturating add), and `take` — no iterator and no setter. Add a set-exact method rather than replacing the component wholesale; wholesale replacement inside a spawn path is the wider blast radius, not the narrower one. The missing iterator blocks harvest the same way: with nothing to enumerate, harvest must clone the whole component wholesale, which `AmmoReserve` supports — it derives `Clone` plus `Serialize`/`Deserialize` with no skipped fields, so whole-component replacement is available. Magazines restore by writing `WeaponComponent.magazine` (`weapon.rs:275`) per slot after composition. Carry nothing whose lifetime is bounded by the pawn instance: no cooldowns, no in-flight reload state, no movement runtime, no death latch, no kill credit. One rule covers all five, and the two that already declare it in source confirm the rule rather than standing alone — `pending_kill_credit` and `contributor_ledger` are `#[serde(skip)]` and documented transient (`entities/src/components/health.rs`). A reload in flight is bounded that way too: the magazine carries, the animation timing that would have filled it does not. Pin the death case AC-CARRY-1 leaves undefined: a carried `health_current` at or below zero clamps to the descriptor default on seed rather than seeding a dead pawn. The carried set must be enumerated in exactly one place using the pattern `networking.md:129` names working today — bind every field and name any skip, so a field added later and not bound is a compile error, not a runtime test (I7).

### Task 6: Roster and the session-state ledger

Build the engine-owned roster and publish it. The roster is keyed by seat and holds, per entry, the seat, the asserted player id if any, a display name, and whether that seat is currently connected. Its vocabulary is closed: it carries lifecycle facts the engine mints or observes, plus the display name the client asserted — and nothing else. **The roster must be reconstructible from the asserted player ids alone** (I5) — session id and seats are host-minted and cannot survive a host change (`plans/roadmap.md:202`), so no roster field may depend on a host-minted value that is not also derivable from its player id. Add the `SessionRoster` variant to `ServerControlMessage`, **appended after `SwitchAccepted`** (`wire.rs:1260`) because bitcode tags enum variants positionally and inserting anywhere else renumbers shipped variants. Adding this variant bumps the app-protocol (message-vocabulary) constant per `networking.md:379-391`; bumping it is a required step of this task. Whether it also bumps `WIRE_VERSION` is a measurement, not an assumption: `ServerControlMessage` has five variants today, and bitcode's discriminant width is a function of variant count. Round-trip the five shipped variants before and after appending the sixth and assert byte-identical encodings. A difference means the layout changed and `WIRE_VERSION` bumps too, independently, per that same section's Version gates rule. Roster changes mark a dirty flag; one publish per poll drains it, after the lifecycle batch, to every slot from `Pending` upward — never from the mutation sites themselves, or a single poll ships three rosters and two of them are intermediates. An admitted or participating slot receives `RosterView::Full`; everything below admitted receives `RosterView::Counts` (I9). `participants` is the count of roster entries with `connected: true`, seat 0 included; a held seat is not counted. Publishing below participating is sound rather than merely tolerated: `NetServer::send_control` and `NetClient::drain_control` (`crates/net/src/transport.rs`) show a non-participating slot does receive control payloads — `send_control`'s own doc says control may be sent to an admitted or closed slot to deliver a hold or reject diagnostic, and `drain_control` returns payload-bearing frames regardless of epoch. So a client held for parity across a long level load watches a live roster instead of a stale one. The message carries the recipient's own seat so a client knows which entry is its own; a pre-admission recipient holds no seat yet and gets `your_seat: None`. Seat *contents* never cross the wire — the net crate stays registry-blind (I8, `development_guide.md:28` plus `crates/net/Cargo.toml`'s dependency list). Add the seat and the roster to the session-state ledger enumeration that `networking.md:149-153` opened with one entry (AC-LEDGER-2). Extend `layering_invariants_hold` (`crates/xtask/src/crate_graph.rs`) with an assertion that `postretro-net` has no internal dependencies, so I8 is enforced rather than merely warranted (AC-BUILD-1). Also fix the placement defect. `SlotPawns.placement_assignments` documents that it "survives a close so a reconnecting client lands on its prior spawn" (`netcode/lifecycle.rs:35-42`) — this part holds: `on_slot_closed` in `netcode/lifecycle.rs` removes only the `pawns` and `wieldables` entries and never touches `placement_assignments`, so the map does survive a close. The real defect is two-part instead. First, `assign_placement`/`placement_assignment` key by client id, while a reconnect mints a fresh client id in `NetEndpoint::from_role`, so the surviving entry is unreachable. Second, `reset_level_scoped_host_state` replaces `SlotPawns` wholesale at level unload, dropping the map, which is not a close. Move placement assignment onto the seat, where reconnect keys by seat rather than client id and level unload no longer drops it. Task 2 lands `placement` as a field on `SeatSessionState` and the cursor `next_assignment` on the seat table; assignment here keys by seat, and the client-id-keyed map plus its reset in `reset_level_scoped_host_state` are deleted. Leaving the cursor behind would assign placement 0 to the first post-level-change joiner, colliding with a seat that already owns it. Carrying it is not sufficient on its own: `assign_placement` issues `next_assignment % placement_count`, so a cursor at 2 entering a two-placement level collides just as hard. Replace the modulo with a scan that skips indices a live seat already holds in this level, and fall back to the modulo only when every index is taken. Correct or delete the comment.

### Task 7: Seat hold and reclaim

Make a dropped player able to come back. Hold-start keys to the seat-unbind edge Task 2 defines — `ServerPoll.disconnects` — never to `SlotEvent::Closed`: `SlotTable::transition` (`crates/net/src/slots.rs`) emits `Closed` only from `Participating`, and a level change demotes every participant to `Admitted`, so a drop during a level load — the likeliest moment to drop — emits no lifecycle event at all. On that edge, mark the seat disconnected in the roster and start the hold window. A relay-added connection closes through `close_relay_connection` rather than a transport disconnect, so it produces no unbind edge: relay seats are released outright, never held. The three binary-side relay harnesses must keep passing under that rule. Do not harvest here: the disconnect despawns the pawn, and Task 2 already hangs the harvest on pawn despawn, so this edge stores nothing itself. On a later admission whose stashed claim matches a held seat's player id exactly, rebind that seat to the new client id and restore its carried state; on no match, or on an expired hold, mint a fresh seat at defaults. If more than one held seat matches — a stale seat from an earlier drop plus a recent one — the most recently held wins and the loser is released immediately rather than left to expire (I1). **This decision happens at exactly one chokepoint** (I4) — the admission edge Task 2 already mints at, so a later auth epic replaces the claim's provenance there and touches nothing else — and matching is whole-identity equality only, never a parse or a partial comparison (I3). Expiry and publish are two different sites, and conflating them strands the roster. Evaluate hold expiry once per sim tick in the game-logic stage, gated on a ticking world so a release never lands mid-install; that site marks the roster dirty when it removes an expired seat (AC-ROSTER-1). The publish itself runs wherever the poll is drained, including `poll_world_less_transport` on Loading and Frontend frames — admissions and disconnects are observed there, so the roster genuinely changes there, and a publish gated on a ticking world would send nothing for the whole inter-level window. `unload_level` sets `BootState::Frontend`, so that window is every level change.

The hold clock is accumulated sim delta, not wall clock. It therefore does not advance while the host sits in Frontend or Loading: a hold survives a level install of any length and a platform suspend of any length, and expiry measures play time rather than absent time. Task 10's injected fixed delta drives it directly. The window's duration is a constant with a placeholder default, settled at the manual verification gate; the gate must reject 0, which releases on the first evaluation and makes AC-REJOIN-1 unreachable. `crates/net/src/slots.rs` needs no change, and closed-slot terminality stays intact. Reclaim rebinds a seat to a **new** client id, and every production connection mints its id at exactly one site — wall-clock nanos, per connection, in `NetEndpoint::from_role`'s `NetRole::Connect` arm — so a reconnecting peer never presents an id `on_connect` has already seen. Relay-added ids come from harness callers rather than that site; they are never reclaimed, so they do not weaken the guarantee. `entry().or_insert()` inserts it as `Pending` normally, `transition`'s early return for a closed slot is never reached, and the never-connected tombstone population is never approached. The stale-packet guarantee is untouched and no shipped contract changes. Note that `close_slot` clears `parity_declarations`, `holding_diagnostics`, and `participation_epochs` (`transport.rs:503-508`), so a rebound connection re-establishes parity from scratch like any new connection — the seat carries gameplay state, not handshake state. Two live connections asserting one identity is not an error: the second gets its own fresh seat, the first is undisturbed, and the host logs it.

### Task 8: Verify the client's ammo display across a carry

No wire change. The client's displayed ammo already follows the host; this task proves it instead of building a second path.

`AmmoSlotProjection::for_pawn` (`crates/postretro/src/netcode/state_slots.rs`) projects both the loaded magazine and the reserve for the active weapon's ammo type into owner-private slots, re-resolving the active wieldable from live `Inventory` every time, so it follows a weapon switch with no cached weapon id. `host_replicate` ingests it once per snapshot batch at 30 Hz. The HUD binds `player.ammo` and `player.ammoReserve` (`content/dev/scripts/hud.ts`) against the snapshot `App::build_ui_slot_snapshot` builds; on a connected client those records arrive via `ClientStateApply::apply_snapshot_state`. The HUD never reads a component. Seat-seeding the host pawn therefore makes the client's display host-authoritative within one snapshot.

The client's locally composed `AmmoReserve` and per-slot `magazine` stay at descriptor defaults and are never corrected. That is not a display defect: nothing shows them. `weapon_hud_values` (`scripting/systems/ui_proxy.rs`) computes them and the connected-client branch discards all but the weapon `EntityId`. Do not put them on the tuning payload — the values would reach no consumer, and `WieldableTuningPayload` is a descriptor-tuning shape rebuilt only on participation entry and mod-content install, so gameplay-rate values behind it would be stale by construction.

Deliver assertions. After a level change and after a rejoin, `player.ammo` and `player.ammoReserve` on the client match the host's authoritative values for the equipped weapon within one snapshot, and both re-target when the player switches weapons. Composition needs no assertion of its own: the client composes from the tuning payload's canonical names (`net_descriptor.rs:207-213`), the host builds that payload from its live inventory, and the Ordering matrix already pins the seat seed ahead of `tuning_payload_for_pawn`. Active slot does need one. It is the carried value with the most competing writers — derived at composition, overwritten by `apply_host_switch_declaration` and by the weapon-switch commands — so assert it lands on the client rather than inferring it from composition.

### Task 9: Split `wire.rs` control region and the composition block

Two small behavior-preserving extractions, sequenced ahead of the tasks that extend them. In `crates/net/src/wire.rs` (2,959 lines, production 1,335), extract the handshake/parity/control region at `:1100-1295` into a `wire/control.rs` submodule — this is exactly where Tasks 1 and 6 add types. `digest_hex` (`:1235`) is used only by `DivergenceReason`'s `Display` and travels with it; `ServerControlFrame` and `ParticipationFrame` are `pub(crate)` and consumed by `transport.rs` (imported at `:19-20`), so the new module must re-export at crate-internal visibility. In `crates/postretro/src/scripting/builtins/data_archetype.rs` (3,035 lines, production 1,013), extract the four composition functions — `seed_weapon_reserve`, `compose_wieldable_inventory`, `compose_wieldable_inventory_from_slots`, `compose_wieldable_inventory_slots` — as a unit, to their closing braces rather than to a line number; `compose_wieldable_inventory_slots` runs past its `active_wieldable` derivation through the `set_component` write that commits the composed `Inventory`. This is where Task 5 lands. One visibility hazard: `compose_wieldable_inventory` is `pub(super)` and is imported by `net_descriptor.rs`, so moving it a module deeper re-points `pub(super)` and must be paired with a widening or a re-export. `compose_wieldable_inventory_from_slots` is already `pub(crate)`; the other two are private with in-file callers only. Both extractions must preserve public and crate-internal paths and produce identical test pass counts.

### Task 10: Carry and rejoin harness coverage

Build the test coverage for the whole flow, which does not exist today. There is no two-process harness: `crates/net/src/harness.rs` is a packet conditioner (delay, jitter, loss on a virtual clock), not a loopback. The in-process harness `LoopbackHarness` (`crates/postretro/src/netcode/predict_reconcile_harness_test_fixtures.rs`, driven by `step` and `step_until_armed`) does not run real endpoints over loopback sockets: `LoopbackHarness::new` binds one ephemeral socket solely to satisfy the `host_handle_client_message` signature — its own in-source comment says no packets ever flow over it — and the file contains no `NetClient` at all, since bytes move through `PacketConditioner`. It has no connect edge, no `user_data`, and no `SlotTable` close. The fixture that constructs both `NetServer` and `NetClient` over real loopback sockets with a shared fingerprint, and therefore owns a real connection lifecycle and a reachable `user_data`, is `relay_pair` in `crates/postretro/src/netcode/trigger_state_channel_harness_test.rs`. Its name misleads: it does not use `add_relay_connection`, though other tests in that same file do, and those cannot carry a claim. Connect/close-dependent coverage either extends `relay_pair` or unit-tests `SeatTable` directly. Extend it to cover: a client connecting with an asserted identity and receiving a seat; a level change preserving that seat and its carried values; a disconnect starting a hold; a rejoin inside the window reclaiming the seat and its state; and a rejoin after expiry getting a fresh seat at defaults. Add the ordering cases that unit tests can cover directly rather than through the harness: harvest with no pawn present, an absent or corrupt claim, two live connections asserting one identity, a drop while demoted mid-level-load, and a peer admitted but never promoted. Add one regression guard: a rejoin arrives on a fresh client id, so `SlotTable` sees an ordinary new slot and the prior closed entry is never revisited. Assert it directly — the whole design rests on reclaim never needing a closed slot reopened. Add an E17 regression leg: trigger occupancy, the Use interaction, and per-player `use_pressed` behave identically for both local and remote players across a level change (AC-E17-1). Add a hygiene leg: repeated connect/disconnect cycles leave the claim stash and seat-table growth bounded (AC-HYGIENE-1). Add a roster-rebuild leg: a roster rebuilt from player ids alone under a fresh session, with an anonymous entry asserted as not rebuildable (AC-ROSTER-2). Add a pre-admission leg: an unadmitted peer's received frames carry `RosterView::Counts` only, never a name or player id (AC-ROSTER-3). Add a ledger leg: the seat and the roster appear in the session-state enumeration (AC-LEDGER-2). Add a single-player leg: a level change carries health, ammo, and loadout with no network endpoint present at all — the path `clear_net_level_parity` cannot reach. Follow the project's test conventions — `<subject>_<verb>_<expected_outcome>` naming, injected fixed delta time rather than wall-clock reads, and exhaustive matches with no wildcard arm so a new state variant becomes a compile error. Any log line an acceptance criterion names becomes contract; keep asserted strings to the criteria that need them.

## Sequencing

**Phase 1 (concurrent):** Task 3, Task 4, Task 9 — behavior-preserving splits of the four files later tasks extend. No functional change, no shared files.

**Phase 2 (sequential):** Task 1 — the claim reaches the host and nothing else changes. Narrowest possible first crossing of the transport seam.

**Phase 3 (sequential):** Task 2 — thin slice; falsifies the boundary assumptions by carrying one value across every seam before anything fans out.

**Phase 4 (concurrent):** Task 5 and Task 6 — the carried set and the roster. Both consume Task 2's seat table and neither reshapes it: Task 2 lands `SeatSessionState`'s full field set and the placement cursor up front, so each fills behavior on fields that already exist.

**Phase 5 (sequential):** Task 7 — hold and reclaim. It marks seats disconnected in the roster and republishes on expiry, so it needs Task 6's roster to exist.

**Phase 6 (sequential):** Task 8 — verification only, no production change. Consumes the seat-seeded host pawn from Task 5.

**Phase 7 (sequential):** Task 10 — consumes every prior phase.

Splits lead rather than following the thin slice because they are behavior-preserving and every subsequent task edits the files they touch; running them after Task 2 would move that task's edits and manufacture conflicts. Task 2 remains the first task that crosses seams and the first that can falsify the design.

All line references in Phases 2 through 7 are pre-split anchors; resolve them by symbol name in the post-split files, since Tasks 3, 4, and 9 move every cited line.

## Manual verification

Orchestration ends with a human pass. Automated coverage proves the mechanism works; this gate settles the one value the mechanism is indifferent to.

**Hold-window duration.** Host a session, drop a player mid-level, and rejoin. The window is long enough when it survives a client relaunch and reconnect at realistic speed, and short enough when an abandoned seat does not hold its spawn placement for the rest of the session. Set the constant to the value that pass produces and replace the placeholder. The epic does not close until it runs.

## Rough sketch

`Seat(u16)` in `crates/foundation`. New wire types in `crates/net/src/wire.rs` (post-split, `wire/control.rs`): `SessionId`, `PlayerClaimId`, `ConnectClaim`, `RosterEntry`, `RosterView`, `SessionRosterMessage`, plus `ServerControlMessage::SessionRoster` appended last.

New module `crates/postretro/src/netcode/seat.rs`: `SeatTable { session_id: SessionId, next_seat: u16, seats: HashMap<Seat, SeatSessionState>, bound: HashMap<Seat, u64>, pawns: HashMap<Seat, EntityId>, claims: HashMap<Seat, Option<ConnectClaim>>, holds: HashMap<Seat, HoldDeadline>, next_assignment: usize }`. The seat stores the whole claim rather than the id alone: the roster publishes a display name for a held seat whose connection is long gone, and Task 1's stash is removed at the disconnect edge. `SeatSessionState { health_current: Option<f32>, reserve: AmmoReserve, wieldables: [Option<String>; WIELDABLE_SLOT_CAPACITY], magazines: [Option<u32>; WIELDABLE_SLOT_CAPACITY], active_slot: usize, placement: Option<usize> }`. Seat 0 is the local player's, minted with the table. The whole `SeatSessionState` field set lands in Task 2 so later tasks fill behavior rather than fields. `next_seat` pins its exhaustion behavior: at `u16::MAX` the allocator refuses the mint, the host logs and closes the slot, and no number is ever returned twice.

Harvest hangs off pawn despawn — the `Closed | Demoted` lifecycle arm and the level teardown both `unload_level` and `App::suspended` reach — ahead of `clear_for_level_unload`. Seed applies inside each spawn path, after its `spawn_descriptor_instance` and composition calls, before the promotion seam reads the inventory, with the carried record passed in as a parameter rather than looked up. Route every seed through one call per spawn path so a later carry policy has a single site to suppress. The seed is a non-destructive read: the seat record is overwritten only by a harvest, never consumed by a seed, so a promotion that produces a fixture pawn with no `HealthComponent` or `Inventory` does not discard the carry. `AmmoReserve` gains a set-exact API or is replaced wholesale — it derives `Serialize`/`Deserialize` with no skipped fields, so either works.

## Open questions

- **Hold-window duration.** Settled at the manual verification gate; see Manual verification. Ships as a constant rather than an authored knob because a knob is lobby-surface work.
- **Display name sourcing.** The id is generated and persisted (Task 1); the name beside it is not. Nothing today asks a player what to call themselves, and the lobby surface that would belongs to the Epic 15 lobby spec, not yet drafted. Until then the client always populates `display_name` from `PlayerOptions` and it is empty. The host never substitutes one: a host-minted name is not derivable from the player id, which I5 forbids.
- **`drafts/E16--per-player-currency` carries a stale decision.** It states that a disconnect releases the seat and drops its values, and that a rejoin is a new seat at defaults — reversed by Task 7, which holds the seat and restores it. That draft also argues against a standalone seat spec and places the seat in `entities`. All three need reconciling there when it unparks; none blocks this spec.
